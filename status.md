# Статус проекта

Обновляется по ходу работы. Дедлайн задания: 4 августа 2026.

## Сделано

- [x] Сайт-отчёт: точная вёрстка по референсу, LIVE на https://serge-mel.surge.sh
      (исходники в `site/`, блок 00 PLANNING раскрывается с планом задания)
- [x] Структура проекта BB заведена с нуля (29.07)
- [x] Репозиторий на GitHub (приватный): https://github.com/sergemeleshchuk-netizen/bubble-word (29.07)
- [x] Играбельный прототип Bubble Words (подводный, drag-merge слов по категориям)
      встроен в блок 04 Build Playable на сайте в виде экрана телефона (29.07):
      `site/playable/index.html`, PLAY-проверен в браузере (мердж PLANET+ROCKET, ходы считаются).
      Позже через него будут прогоняться уровни, сгенерированные инструментом.
      30.07: рамка увеличена до пропорций реального телефона 390x844 (~88vh при
      раскрытии); физика пузырей починена (дрожание и наслоение: мягкое
      расталкивание 25%/итерацию, покой гасит скорость без отката позиции,
      гравитация не дёргает лежащих). Замер в браузере: движение улёгшейся
      кучи 0 px, пересечений глубже 90% радиусов нет, мердж раздвигает соседей мягко.
- [x] Пайплайн слов от ChatGPT (`level-generator/bubble_jam_pipeline/`) запущен в режиме
      wordnet-only (29.07): 114 555 групп / 8 731 SAFE, но это вариации ~1,5 тыс. концептов;
      качество слабое (абстрактные категории, в SAFE попали ругательства и слуры).
      Вердикт: использовать только как сырьё «категория → пул слов» после курирования
      и обязательного блок-листа; поля конфига min_zipf/max_word_length кодом не читаются.
      Результаты в `level-generator/bubble_jam_pipeline/output/` (в git не коммитим).

- [x] Этап 0 закрыт (30.07): 4 записи геймплея уровней 1-20 в `reference/`
      разобраны по кадрам; параметры референса в `reference-analysis.md`
      (категория = 4 слова, имена скрыты до сборки, лимит ходов = 3*M*K,
      поле ~20-24 пузыря с досыпкой, хронология механик chunks/chains).
      Схема уровня и кривая в `approach.md` обновлены под референс.

- [x] Шаг 1 закрыт (30.07): база слов `tool/data/categories.json` - 175 категорий,
      1125 уникальных слов (пулы 4-8 слов, ядро из категорий референса LVL1-20
      + авторское расширение), аннотация zipf-частотностью, индекс 71 слова
      в двух+ категориях (кандидаты в ловушки: bark, cold, cream, hat, orange...).
      Блок-лист `tool/data/blocklist.txt` (~150 записей). Сборка и валидация:
      `tool/scripts/build_base.py` (запуск из venv пайплайна), отчёт `tool/data/base_report.md`.

- [x] Скилл `word-associations-generator` (30.07, `.claude/skills/`): генерит
      категории слов по теме/примеру и наполняет базу через build_base.py.
      Проверен боевым прогоном: тема «путешествия» -> +4 категории (airport,
      hotel, pirates, picnic), 9 новых ловушек-пересечений (basket, blanket,
      key, compass, parrot...). База: 179 категорий, 1144 уникальных слова.

- [x] Шаг 2 закрыт (30.07): схема уровня зафиксирована в `levels/SCHEMA.md`
      (инварианты + поля), 3 эталонных уровня руками в `levels/etalon/`
      (e1 D1: 5 далёких категорий K=1.8; e2 D5: 8 категорий, ловушки orange/bark,
      K=1.4; e3 D9 спайк: смежные пары фрукты/ягоды и сладости/выпечка,
      4 ловушки, повтор milk, K=1.25). Валидатор `tool/scripts/validate_level.py`
      (4 слова в категории, ловушки по базе, незаявленные пересечения, формула
      лимита, repeats по истории пакета). Playable научен грузить уровень из
      `?level=<путь к JSON>`: лимит/досыпка из board, режим «без лимита»,
      имя категории появляется на финальном оранжевом пузыре. PLAY-проверено
      в браузере: все 3 уровня грузятся, драг-мердж работает, бот-стратегия
      проходит с остатком 12/10/7 ходов.

- [x] Шаг 3 закрыт (30.07): скилл `level-generator` (`.claude/skills/`) -
      собирает уровни из базы под целевую сложность D1-10 (таблица ручек:
      категории/ловушки/смежность/K), ловушки только из пересечений базы,
      пакет планируется целиком (повторы, спайк). Боевой прогон: уровень
      `levels/demo/journey-d7.json` (10 категорий, ловушки parrot/turtle
      на pets, 4 водных категории, K=1.3) - валидатор OK, бот проходит
      с остатком 9 из 39 ходов.

- [x] ТЗ зафиксировано (30.07): `GDD.md` в корне BB (GDD Word Bubble Jam по референсу:
      ядро, физика, бустеры, контент-пайплайн, прогрессия механик); содержимое
      выложено на сайт в блок «03 GDD Readme MD», задеплоено на serge-mel.surge.sh.
      Ред.2 (30.07, решения по ревью): единая формула лимита
      move_limit = ceil((3*M + chunks) * K), K 1.6-1.25, спайк = ходы бота - 1;
      счётчик X/M вместо X/12; досыпка штатная + внеочередная (анти-тупик)
      + гарантия генератора от тупика; «ровно 4 слова» с единственным
      исключением мета-категорий (прогресс: ярлык +1 сразу, мета входит в M);
      склейка половинок тратит ход (chunks), половинка = порядок 0.5 голубой,
      запрет валидных слов из чужих половинок. Решено НЕ трогать: магнит
      бесплатный как есть, граница промах/пустота без формализации,
      подсветка при драге = партикл-след (не подсказка), лёд и стартовая
      четвёрка без уточнений.

- [x] Шаг 4 закрыт (30.07): скилл `level-solver` (`.claude/skills/`) - слепой
      решатель. Пайплайн: `tool/scripts/solver_blind.py` (только имена категорий
      + перемешанные слова, детерминированный seed) -> независимый субагент
      раскладывает без доступа к файлам -> `tool/scripts/solver_check.py`
      (exit 0 PASS / 1 FAIL=брак / 2 структурная ошибка; отчёт
      `levels/solver/<имя>.report.md`: точность, вердикт по каждой ловушке,
      hesitations+confidence как эмпирика для шага 5). Боевой прогон: e1/e2/
      journey-d7 PASS; e3 решатель ЗАБРАКОВАЛ ДВАЖДЫ (первый отчёт «все PASS»
      был ошибкой: код возврата проверялся после tail; поймано на шаге 5).
      FAIL 1: cookie-donut раскладывались двояко (donut в пуле sweets базы
      отсутствует, поэтому cookie переехал в baked-goods по логике решателя,
      donut убран, sweets добрал lollipop). FAIL 2: grape-cherry симметричная
      двусмысленность (grape ботанически ягода) - grape заменён на pear.
      Ред.3 e3: PASS 36/36, все 4 ловушки валидны и «сработали». Негативные
      ветки solver_check проверены синтетикой (подмена слов = FAIL,
      потерянное слово = структурная ошибка).

- [x] Безлимитный источник базы (30.07): выгружены ответы оригинала L1-199
      (`reference/bwj-answers/`, локально: 2244 категории, 4793 слова,
      разбор bwj_report.md; достоверность 20/20 с видео-разбором); из
      выгрузки в базу влито 19 категорий (сезоны с мета-парами, meats,
      morning-routine...) - база 198 категорий / 1233 слова / 108 пересечений.
      Докачка = штатный режим скиллов: word-associations-generator получил
      режимы augment (ядро 4 слов из источника + LLM-досыпка до 6-8) и
      дочерпку `tool/scripts/mine_bwj.py` (пачки кандидатов, без состояния:
      влитое исчезает из выдачи через базу; фильтр близнецов по пересечению
      пулов; в очереди ~1050 кандидатов). Контроль качества при массовой
      докачке замыкает слепой решатель (шаг 4).

- [x] Шаг 5 закрыт (30.07): модель оценки зафиксирована в `levels/EVAL.md`
      (живой файл, итерации = коммит + повторная калибровка): две шкалы 1-10,
      D сложность (7 факторов: масштаб, валидные ловушки, эмпирическая
      смежность, редкость zipf<3.0, K, повторы, внеплановая двусмысленность)
      и F фан (якоря от базы 5: узнаваемость, «ага-моменты» = ловушка укусила
      но честно, разнообразие/сюжет, штрафы за спорность вне замысла,
      темп; solver FAIL = фан <= 3, уровень бракован). Скилл `level-evaluator`
      (`.claude/skills/`) + метрики `tool/scripts/eval_metrics.py`.
      Калибровка (допуск +-1): e1 1.0/цель 1 (F 7.0), e2 5.0/5 (F 8.0),
      e3 8.5/9 (F 9.0), journey-d7 7.5/7 (F 7.5); отчёты в `levels/eval/`.
      Подход опубликован на сайте в блоке «05 Level Design Tool».

- [x] SWOW внедрён (30.07): живые ассоциации людей как мера ловушек.
      Датасет SWOW-EN 2018 (1.23 млн ответов, локально в `reference/swow/`,
      research-only лицензия) -> `tool/scripts/swow_source.py` (build/word/
      traps/mine). Сила ловушки = средняя sym-связь слова со словами пула.
      Замер: orange->fruit 0.206/color 0.089; cold к температуре 0.227 против
      дома-болезни 0.063; 34 сильных ловушки в базе, mine дал 1966 новых
      кандидатов (топ - мета-пары dog/dog-breeds 0.348, cheese/cheeses 0.337).
      Скиллы обновлены (SWOW-проверка в процедурах), Research-блок сайта
      дополнен и задеплоен. ConceptNet отложен: API мёртв (502), дамп 1-2 ГБ
      подключаем только если не хватит качества пулов на масштабе.

- [x] Масштаб «следующие 100 уровней» (30.07): резервуар SWOW 14 092 слова
      вне базы (`swow_source.py reservoir/pool/cats`: 6317 стимулов-кандидатов
      в категории, ранжирование по связности); +14 категорий из ассоциаций
      (jewelry, harbor, police, detective, fishing, barbecue... 8 ловушек) -
      база 212 категорий / 1307 слов / 115 пересечений. Сборщик пакетов
      `tool/scripts/assemble_pack.py` (план -> уровни: ловушки в home, охрана
      от незаявленных двусмысленностей, авто-repeats, формула лимита).
      Демо-пакет `levels/packs/volume1/` L200-209 по кривой оригинала
      (12-16 категорий, спайк L209 K=1.25, мета-пара winter/seasons,
      повторы milk/green/sun/bass): валидатор чисто, слепой решатель
      PASS 10/10 после 2 итераций починки. Урок в скиллы: «неразделимые
      пары» категорий (school-supplies+stationery, space+astronomy) на
      один уровень не ставить - слова двоятся; решатель это ловит.

- [x] Пакет volume2 (30.07): L210-219 с НУЛЕВЫМ пересечением с референсом
      (проверено скриптом: 0 общих слов из 4793 и 0 общих имён из 1660).
      В базу добавлено 94 новых темы «поздней палитры» (балет, кузня, зелья,
      сейсмология...) - база 306 категорий / 1751 слово. Ловушки из
      инженерных пересечений (sparring, vial, horseshoe, lasso). Слепой
      решатель PASS 10/10 с первого прогона; уровни в селекторе сайта,
      прод проверен ботом.

- [x] Перепроверка пакетов volume1+volume2 (31.07): у 8 из 10 уровней volume1
      solution был СТАРШЕ файла уровня (правки после прогона) - все 8
      перепрогнаны свежими независимыми решателями. l202 на свежем прогоне
      FAIL: june/july/august симметрично делились между Months и Summer;
      починка - Months = march/april/may/october (без летних месяцев),
      august единственный летний на уровне, ловушки june/july сняты (осталось
      2: august, glass). Итог: 20/20 PASS, вердикты volume2 пересверены
      реальными кодами возврата. Валидатор 20/20 OK (warn l204: ловушка
      winter базой не подтверждена). Правило в работу: перед доверием
      вердикту сверять mtime solution vs level (свежесть прогона).

- [x] Тестовая группа T1-T10 в playable (31.07): выгрузка инструмента
      (`game-pack-1-10.json`, gen-1.0) сохранена в `levels/packs/tool-test/`,
      сконвертирована в формат playable скриптом `tool/scripts/pack_to_playable.py`
      (schema 2.0 -> SCHEMA.md; поле берём из `board_capacity`, а не из
      `start_bubbles`, который в пакете означает число спавнящихся слов).
      В селекторе сайта отдельная группа. Браузер: T1 и T5 грузятся, счётчики
      0/13 и 0/18, ходы 57 и 68.

- [x] Исправления по внешнему аудиту базы слов (31.07): аудит
      (`word_database_audit`, ChatGPT Pro по снимку SQLite) разобран, план и отчёт —
      `tool/word_content_pipeline/AUDIT_RESPONSE.md`. Все P0 и P1 закрыты в пайплайне
      и текстовых источниках, база пересобрана с нуля.
      **P0:** гейт «нет частотности — нет approved» (было 20 таких approved, стало 0);
      96 связей многозначных слов получили значение через новый
      `data/seed/_sense_map.json` (85 явно + 11 объявленное исключение для категорий
      игры слов, где играет написание, а не смысл); механизм `sense-gaps` (556 слов
      в очереди на разведение значений); `04_flags.md` больше не обрезается молча
      (было 60 категорий из 86, стало 21 из 21) и получил разделы про частотность,
      approved и риски; проверка пригодности категории для четвёрки; 516 запретов
      на сочетание категорий; 18 семантических правок в seed (`gypsy moth` ->
      `spongy moth`, убраны `Eskimo Pie`, `kabuki`, `penguin` из ARCTIC ANIMALS,
      `ringworm`, `wake`, `blink`, `calfling`, INSECTS -> BUGS и др.);
      категории без нормальной четвёрки: было 34, стало 5.
      **P1:** разделены четыре оси — `semantic_status`, `familiarity_score`,
      `obviousness_score`, `gameplay_difficulty`; статусы пересчитаны по абсолютным
      порогам (SWOW стал вторичным сигналом — раньше относительное сравнение
      категорий слова загоняло `xylophone` в MUSICAL INSTRUMENTS и все пустыни
      в `hard_only`); слой проверенных четвёрок (3005, solver единственности);
      `readiness` категорий; парная структура OPPOSITES.
      **P2 частично:** `PRAGMA user_version = 2` + `schema_meta` (commit, хеши
      источников, версия wordfreq); слой рисков заполнен (5557 связей, было 0).
      Проверки: `check-integrity` — все 10 блокирующих OK; тестов 93 -> 123.
      Ручные решения вынесены в `БАЗА-СЛОВ/ревью/06_manual_decisions.md`.

- [x] Одна база вместо двух (31.07): у проекта было ДВА источника контента -
      SQLite-пайплайн `tool/word_content_pipeline` (аудированный) и его локальная
      копия `tool/level-tool/pipeline`, из которой веб-инструмент брал снимок.
      Копия осталась на состоянии до аудита (approved 9136 против 12598, слоёв
      readiness/конфликтов/четвёрок нет вовсе), а её база к этому моменту опустела.
      Сделано: экспорт снимка переключён на канонический пайплайн, схема снимка
      **snapshot-2.0** (readiness, 517 запретов пар, risk-флаги, gameplay_difficulty,
      3006 четвёрок); прогон мета-хабов (26 категорий, STARGAZING держит слово
      `planets`) перенесён из копии в `data/runs/` канонического пайплайна как
      источник с провенансом - без него мета-категорий было 15 вместо 120;
      пересборка одной командой `scripts/rebuild_all.sh` (init-db старую базу
      НЕ чистит, поэтому шаг «пустая база» теперь честно пустой); карта значений
      `_sense_map.json` применяется на всех путях импорта, а не только в build_seed.
      База: 1120 категорий, 10112 слов, 17786 связей, приёмка 10/10 OK, тестов 126.

- [x] Генератор исполняет решения контента (31.07): жёсткий гейт по 517 парам
      `do_not_pair` в отборе категорий, фильтр по `readiness` (curated_only,
      hard_only, blocked в автосборку не идут), hard-проверка `CONFLICT_PAIR`
      в валидаторе для уровней, собранных не генератором. Плюс генератор наконец
      читает `base_difficulty`: на туториале потолок 0.2, дальше предпочтение
      при отборе. Мета-пар теперь ровно столько, сколько просит план - было
      8 декад из 20 мимо приёмки по META_RANGE, стало 20 из 20. Тестов 57 -> 68.

- [x] Калибровка генератора закрыта (31.07): блок 1-10 пересобран на аудированной
      базе, замер в `levels/packs/tool-test/DIFF-VS-REF.md`. Категорий на уровень
      14.5 -> **8.5** (референс 9.5), медиана zipf 3.58 -> **4.19** (4.35), редких
      слов 20% -> **3%** (5%), многословных 14% -> **0%**, категорий из базы
      22/145 -> **85/85**, неразделимых пар 8 -> **0**, L1 стал туториалом без
      лимита со всеми 20 словами на поле. Приёмка по декаде PASS по всем
      13 проверкам, валидатор 10/10, у каждого уровня ровно одно решение.
      Уровень 1: DISHES / FRUITS / FARM ANIMALS / MONTHS / WEATHER WORDS.
      Playable-группа **C** на сайте пересобрана, проверена в браузере
      (C1 - бесконечные ходы, мердж LEMON+APPLE; C5 - 45 ходов, 0/12).
      Группа T оставлена как «до калибровки» для сравнения.

- [x] Скиллы и скрипты на большой базе (31.07): `tool/data/categories.json`
      больше не редактируется руками - это выгрузка SQLite
      (`tool/scripts/export_base_json.py`): 1394 категории (1120 из базы +
      274 прежних, чтобы сданные пакеты продолжали проверяться), 8459 слов-домов,
      поля `alt`/`conflicts`/`readiness`/`familiarity` сверх прежнего формата.
      Прежняя база - `categories_legacy.json`, `build_base.py` помечен устаревшим.
      Скиллы `level-generator` и `word-associations-generator` переписаны под
      новый источник (второй пишет в `data/seed/*.txt` пайплайна и пересобирает
      базу `rebuild_all.sh`, а не в старый сид). Проверка: валидатор проходит
      все четыре пакета (etalon, demo, volume1, volume2), eval_metrics на e2
      даёт те же цифры, что при калибровке.

- [x] Импорт словаря оригинала L1-1025 и ОТКАТ (31.07): найден второй фанатский
      источник ответов `bubblewordjam.org` — 1025 уровней против 199 у прежнего,
      с разметкой вложенности категорий. Выгружен целиком
      (`tool/scripts/scrape_bwj_org.py`, 12 367 категорий-вхождений, 15 155 слов,
      137 из 199 уровней совпали с прежним источником дословно) и влит в базу
      прогоном `run-002-bwj-org`: 6432 категории, 18 370 слов.
      **Откачено по решению владельца**: чужой словарь в нашей базе не храним.
      Замер, который это решение подтверждает: импорт уронил медиану частотности
      базы с 3.58 до 3.45 и поднял долю редких слов с 27% до 33%, то есть планку
      узнаваемости на импорте я не держал; метрики блока не сдвинулись только
      потому, что фильтрует генератор на выходе, а не база на входе.
      Выгрузка живёт как референс в `reference/bwj-org/` (там же готовые файлы
      прогона — влить «для полноты» можно позже одной командой).
      Инструменты остались: скрапер, конвертер в прогон, `swow_status --categories`,
      применение `_sense_map.json` на всех путях импорта.
      Уроки в коде: пул 6-8 слов — условие приёмки КОНТЕНТА, а не запрет
      в генераторе (порог в генераторе отсекал мета-детей и ронял 3 декады);
      сложность категории нельзя выводить из частотности слов (CARDINAL с пулом
      `robin, bird, direction, vatican` уехала на туториал — поймано в браузере).
      База после отката: 1120 категорий, 10 112 слов, приёмка 11/11, блок 1-10
      воспроизводится с прежним хешем, 20 декад из 20, тестов 68.

- [x] База -> контентная система, фазы 0-3 задания
      `prompts/word-db-production-upgrade-v2.md` (31.07). Главное: проверка
      «четвёрка уникальна» никогда не доказывала уникальность уровня —
      неоднозначность рождается на стыке групп. `rose/tulip/lily/daisy` и
      `red/blue/green/violet` — две безупречные по отдельности четвёрки, а
      уровень из них собирается двумя способами. Появился exact-cover solver
      полного уровня: работает с токенами (слово + значение), знает структурные
      категории, останавливается на втором решении, умеет таймаут и сохраняет
      отчёт о каждом запуске. Таймаут уникальностью не считается.
      Переименования, чтобы поле называлось тем, чем является:
      `quartet_is_unique` -> `quartet_locally_unique`, `quartets.review_state`
      -> `validation_state` (без `human_approved`), `solver_state` ->
      `local_check`. Над категориями надстроен слой семантического принципа
      (concept/variant/alias), уровень стал сущностью базы вместе с журналом
      решений. Ручная проверка теперь ровно в одном месте: человек принимает
      собранный уровень целиком, а не подтверждает 18 тысяч связей по одной;
      причина отказа возвращается точечно на связь, четвёрку или категорию.
      Значения разведены у bell/rose/cricket/iris/jasmine/charger/life/spirit
      и заведены глагольные значения для категорий-действий.
      Схема 2 -> 4 двумя миграциями, тестов 126 -> 177, пять уровней собраны
      и ждут приёмки в `tool/word_content_pipeline/data/content/level_review/`.
      Разбор — `tool/word_content_pipeline/docs/`.

- [x] Рейтинги качества контента и профили генерации (31.07, дополнение к
      заданию выше — `prompts/word-db-quality-scoring.md`). Генератор умел
      собирать корректные уровни, но не умел собирать приятные: отбирая по
      связности, он брал самые удобные четвёрки и делал уровни из четырёх
      банальных слов. Теперь у слова, названия категории и четвёрки есть
      измеримые параметры — длина, вместимость в пузырь, сложность написания,
      знакомость, новизна, неоднозначность, ясность, интересность — а итоговые
      баллы считаются версионируемыми формулами из конфига. Понятность и
      интересность намеренно на разных осях: скучный уровень и перегруженный
      дают одинаковый средний балл, если ось одна.
      Дублирующих полей не заведено: три параметра четвёрки уже были в базе
      под своими именами и переиспользованы (разбор — `docs/scoring_mapping.md`).
      Три профиля генерации (`easy_accessible` / `accessible_fun` /
      `hard_knowledge`) фильтруют по отдельным параметрам и держат бюджеты
      уровня на редкие слова, длинные фразы и неоднозначность. Замер: лёгкий
      профиль даёт минимальную знакомость 0.567, сложный — 0.381.
      Команды `score-*` и `explain-*` показывают вклад каждого компонента,
      а не одно число. Схема 4 -> 5, тестов 177 -> 260.

- [x] Phase 0 единой системы оценки D/F/R/C (31.07, задание
      `prompts/level-difficulty-fun-pack/`). Отдельный слой поверх текущей
      работы: базу и её пайплайн не переписывали, приёмку не останавливали.
      Главная находка baseline: сложность уровня в проекте считалась **трижды**
      (`scoringDifficulty.ts`, `difficulty.py`, `levels/EVAL.md`), «интересность»
      и «фан» — два имени одной оси, а ambiguity живёт под тремя разными именами
      на трёх уровнях. Ещё один дубль: ловушка и смежность категорий в коде
      могут посчитать один и тот же конфликт дважды — правило дедупликации есть
      только в документе.
      Mapping решил это без новых дублей: единственной моделью D/F/R/C
      становится level-tool, `difficulty.py` остаётся эвристикой отбора
      кандидатов (помечено в docstring), `EVAL.md` — объяснением для человека.
      Из 27 требуемых параметров слова/названия/связи/четвёрки в базе уже есть
      24; все шесть статусов жизненного цикла уровня совпали один в один,
      второго словаря не заводим. Fairness Gate выводится из имеющейся
      разметки hard/soft, а не как второй результат валидации: 8 из 9
      обязательных gates уже реализованы hard-проверками.
      Реально нового: `R`, `C`, structural metrics (§8 — ни одной метрики поля
      сейчас нет), каталог механик, snapshot оценок на принятом уровне.
      Разбор — `tool/docs/level_evaluation/`.

- [x] Инструмент упрощён до словесного/семантического (01.08). Убрано с экранов:
      «Типы связи» с базы, включение/исключение тематических сфер с настройки
      (58 сфер в две прокручиваемые колонки всё равно не выбирались осмысленно),
      закладка Playable (прототип — отдельный пункт сайта).
      **Модификаторы выключены не только в интерфейсе, но и в генерации:**
      реализована была одна цепь, а одна механика не образует системы — она
      добавляет к сложности то, что нечем уравновесить, и мутит оценку D.
      `allowedModifiers` пуст в дефолте и в профилях декад; замер декад
      по-прежнему знает, где в оригинале появляются цепи, но генератор их не
      ставит. Разбор свободного пожелания перестал понимать «цепи»: понимать
      просьбу и не исполнять её хуже, чем честно отправить в «не понято».
      Следствие: хеши пакетов изменились — пресет 201-210 собирает другие уровни.

- [x] Кнопка «Добавить в Playable» на экране экспорта (01.08). Пакет уезжает
      в прототип через общее хранилище браузера (инструмент и прототип на одном
      origin) и появляется там первой группой в списке уровней, названной
      версией инструмента и хешем пакета — «v1.4.0 · 4707d962b20b».
      Между «собрал уровень» и «сыграл в него» больше нет ни файлов, ни деплоя;
      раньше путь шёл через скачивание JSON, укладку в `site/playable/levels/`
      и деплой сайта — на этом проверка идей и умирала.
      Форма пакета закреплена тестом: её читает обычный HTML сайта, которого
      не видит компилятор. Инструмент 1.3.0 -> 1.4.0, тестов 95 -> 100.

- [x] Инструмент опубликован на сайте отчёта (31.07, встроен 01.08):
      живёт по адресу `serge-mel.surge.sh/tool/` и **встроен прямо в блок 05
      «Level Design Tool»** — раскрыл пункт и работаешь, без перехода по ссылке.
      Блок поднят над «Build Playable»: сначала инструмент, потом прототип.
      Тяжёлый бандл грузится при первом раскрытии пункта, а не при загрузке
      отчёта. Отдельный домен
      `bubble-level-tool.surge.sh` больше не целевой. Собранный инструмент
      коммитится в `site/tool/`, чтобы сайт деплоился из репозитория без
      предварительной сборки; команды `build:site` / `deploy:site`.

- [x] Форма настройки блока применяется кнопкой, а не по ходу ввода (31.07).
      Причина не в удобстве: смена диапазона уровней пересобирает весь профиль
      декады, поэтому заполнив коридор и редкость, а потом поправив диапазон,
      человек молча терял только что введённое — **порядок заполнения формы
      влиял на результат**. Теперь правки идут в черновик, до кнопки не влияют
      ни на что, график ритма под формой — предпросмотр черновика, а
      непринятый черновик видно (подпись и «Вернуть применённое»).
      Инструмент 1.2.0 -> 1.3.0.

- [x] Версионность инструмента (31.07). Версии лежали в трёх местах, у счётчика
      решений её не было вовсе. Теперь один реестр `web/src/core/version.ts`
      (инструмент, генератор, солвер, модели D и F, хеш снимка) + журнал
      `tool/level-tool/CHANGELOG.md`, версия видна в шапке. `TOOL_VERSION`
      сознательно **не входит** в хеш уровня — иначе релиз вёрстки менял бы
      хеши сданных пакетов; это закреплено тестом, как и запрет поднять версию
      без строки в журнале. Инструмент 1.0.0 -> 1.1.0, тестов 82 -> 87.

## В работе

- [ ] Phase 1-4 системы D/F/R/C: технические gates `PASS/WARNING/FAIL`, четыре
      оси с компонентами, structural metrics, blind evaluation, review pack.
      По договорённости начинается после завершения приёмки пяти кандидатов;
      интеграция с массовым генератором и кампанией (Phase 5) — только после
      того, как тесты базовой модели проходят. План — `evaluation_mapping.md` §12.
- [ ] **Отложено на отдельную сессию по решению продакта (31.07):** проблема
      воспроизведения уровней признана слишком серьёзной, чтобы решать её
      попутно. Замер и план сделаны и лежат готовыми — ниже.
      Собрать уровни 1-10 референса прямым запросом к генератору и сравнить
      с записью. Единственное место в проекте, где ответ известен заранее
      (`reference/bwj-answers/bwj_levels.json` разобран с видео), поэтому это
      честная проверка пригодности базы, а не ощущение.
      Замер уже сделан и воспроизводим (`measure_reference_coverage.py`):
      слова есть на 98%, концепты категорий на 80%, а **связи слово→категория,
      которыми и собирается четвёрка, — только на 49%**. Ни один уровень не
      воспроизводится целиком, включая туториальный первый: `compass` и
      `days of the week` в базе отсутствуют вовсе.
      Главная находка не техническая: у 39 из 95 категорий наше имя **уже**,
      чем у оригинала (`school` против `school supplies` / `school subjects`).
      Оригинал берёт короткие бытовые ярлыки, мы декомпозировали темы на
      точные — это лучше для доказуемости и хуже для узнаваемости. Что тут
      правильно, решается продуктово. Разбор и план —
      `tool/docs/reference_reproduction/README.md`.

- [ ] Шаг 6 (approach.md): веб-инструмент (UI на surge + серверный прокси для ключа AI)
- [ ] СВОЯ база того же масштаба на основе выученного на референсе: таксономия
      категорий с вложенностью (мета-пузыри), пулы 6-8 слов, кривая по декадам.
      Строится AI-проходами пайплайна, а не копированием. Не начато.
- [ ] Слепой решатель по новому блоку C1-C10 не прогонялся: счётчик решений
      инструмента даёт 1 у всех десяти, но это не независимая проверка.
      Перед сдачей пакета прогон нужен.
- [ ] Пакет 201-210 остаётся артефактом прежнего снимка: `pack_hash` включает
      хеш снимка контента, поэтому из текущей базы он не воспроизводится.
      Если нужен воспроизводимый - пересобрать и заново прогнать решателем.

## Отложено (опционально, неактивно)

- [ ] ~~Приёмка пяти уровней-кандидатов базы~~ — **снято с плана 31.07 по
      решению продакта: приёмка делается на генераторе уровней, а не на базе.**
      Файл `level_decisions.csv` и команда `apply-level-decisions` остаются
      рабочими, ничего не удалено — задача просто не выполняется.
      Следствие, которое надо помнить: `level_instances` остаются в статусе
      `review_pending`, принятых уровней ноль, поэтому калибровочный набор
      для моделей D/F/R/C собирается **не из принятых уровней базы**, а из
      четырёх эталонов (`levels/etalon/e1-e3`, `levels/demo/journey-d7`) и из
      приёмки генератора. Phase 6 слоя оценки опирается на них.

## Впереди (план из блока 00 PLANNING)

- [ ] Играбельный прототип механики (4-6 h)
- [ ] MD-файлы для солвера уровней (2 h)
- [ ] Скилл Level Solver (4 h)
- [ ] MD-файлы для генератора уровней (2 h)
- [ ] Скилл Level Generator (4 h)
- [ ] Скилл оценки сложности уровней (3 h)
- [ ] Скилл JSON Export (2 h)
- [ ] Финал: 10 уровней с оценками, описание AI-воркфлоу, сайт-отчёт целиком (8 h)

## Решения

- Подход к генератору уровней зафиксирован в `approach.md` (29.07): гибрид
  (LLM придумывает, база+фильтры проверяют), слова-ловушки с ростом сложности,
  параметры поля снимаем с записей референса (ждём материалы в `reference/`).

- Хостинг инструмента: статическая страница на surge + серверный прокси для ключа AI.
- Домен инструмента: отдельный, не смешиваем с другими проектами.
- Репозиторий: https://github.com/sergemeleshchuk-netizen/bubble-word (приватный; перед сдачей открыть доступ проверяющему или сделать публичным).

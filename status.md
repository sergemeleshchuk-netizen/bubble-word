# Статус проекта

Обновляется по ходу работы. Дедлайн задания: 4 августа 2026.

## Сделано

- [x] Сайт-отчёт: точная вёрстка по референсу, LIVE на https://serge-mel.surge.sh
      (исходники в `site/`, блок 00 PLANNING раскрывается с планом задания)
- [x] Структура проекта BB заведена с нуля (29.07)
- [x] Репозиторий на GitHub (приватный): https://github.com/sergemeleshchuk-netizen/bubble-word (29.07)
- [x] Играбельный прототип Bubble Words (подводный, drag-merge слов по категориям)
      встроен в блок 04 Build Playable на сайте в виде экрана телефона (29.07):
      `site/playable/index.html`, PLAY-проверен в браузере (мердж PLANET+ROCKET, ходы считаются).
      Позже через него будут прогоняться уровни, сгенерированные инструментом.
      30.07: рамка увеличена до пропорций реального телефона 390x844 (~88vh при
      раскрытии); физика пузырей починена (дрожание и наслоение: мягкое
      расталкивание 25%/итерацию, покой гасит скорость без отката позиции,
      гравитация не дёргает лежащих). Замер в браузере: движение улёгшейся
      кучи 0 px, пересечений глубже 90% радиусов нет, мердж раздвигает соседей мягко.
- [x] Пайплайн слов от ChatGPT (`level-generator/bubble_jam_pipeline/`) запущен в режиме
      wordnet-only (29.07): 114 555 групп / 8 731 SAFE, но это вариации ~1,5 тыс. концептов;
      качество слабое (абстрактные категории, в SAFE попали ругательства и слуры).
      Вердикт: использовать только как сырьё «категория → пул слов» после курирования
      и обязательного блок-листа; поля конфига min_zipf/max_word_length кодом не читаются.
      Результаты в `level-generator/bubble_jam_pipeline/output/` (в git не коммитим).

- [x] Этап 0 закрыт (30.07): 4 записи геймплея уровней 1-20 в `reference/`
      разобраны по кадрам; параметры референса в `reference-analysis.md`
      (категория = 4 слова, имена скрыты до сборки, лимит ходов = 3*M*K,
      поле ~20-24 пузыря с досыпкой, хронология механик chunks/chains).
      Схема уровня и кривая в `approach.md` обновлены под референс.

- [x] Шаг 1 закрыт (30.07): база слов `tool/data/categories.json` - 175 категорий,
      1125 уникальных слов (пулы 4-8 слов, ядро из категорий референса LVL1-20
      + авторское расширение), аннотация zipf-частотностью, индекс 71 слова
      в двух+ категориях (кандидаты в ловушки: bark, cold, cream, hat, orange...).
      Блок-лист `tool/data/blocklist.txt` (~150 записей). Сборка и валидация:
      `tool/scripts/build_base.py` (запуск из venv пайплайна), отчёт `tool/data/base_report.md`.

- [x] Скилл `word-associations-generator` (30.07, `.claude/skills/`): генерит
      категории слов по теме/примеру и наполняет базу через build_base.py.
      Проверен боевым прогоном: тема «путешествия» -> +4 категории (airport,
      hotel, pirates, picnic), 9 новых ловушек-пересечений (basket, blanket,
      key, compass, parrot...). База: 179 категорий, 1144 уникальных слова.

- [x] Шаг 2 закрыт (30.07): схема уровня зафиксирована в `levels/SCHEMA.md`
      (инварианты + поля), 3 эталонных уровня руками в `levels/etalon/`
      (e1 D1: 5 далёких категорий K=1.8; e2 D5: 8 категорий, ловушки orange/bark,
      K=1.4; e3 D9 спайк: смежные пары фрукты/ягоды и сладости/выпечка,
      4 ловушки, повтор milk, K=1.25). Валидатор `tool/scripts/validate_level.py`
      (4 слова в категории, ловушки по базе, незаявленные пересечения, формула
      лимита, repeats по истории пакета). Playable научен грузить уровень из
      `?level=<путь к JSON>`: лимит/досыпка из board, режим «без лимита»,
      имя категории появляется на финальном оранжевом пузыре. PLAY-проверено
      в браузере: все 3 уровня грузятся, драг-мердж работает, бот-стратегия
      проходит с остатком 12/10/7 ходов.

- [x] Шаг 3 закрыт (30.07): скилл `level-generator` (`.claude/skills/`) -
      собирает уровни из базы под целевую сложность D1-10 (таблица ручек:
      категории/ловушки/смежность/K), ловушки только из пересечений базы,
      пакет планируется целиком (повторы, спайк). Боевой прогон: уровень
      `levels/demo/journey-d7.json` (10 категорий, ловушки parrot/turtle
      на pets, 4 водных категории, K=1.3) - валидатор OK, бот проходит
      с остатком 9 из 39 ходов.

- [x] ТЗ зафиксировано (30.07): `GDD.md` в корне BB (GDD Word Bubble Jam по референсу:
      ядро, физика, бустеры, контент-пайплайн, прогрессия механик); содержимое
      выложено на сайт в блок «03 GDD Readme MD», задеплоено на serge-mel.surge.sh.
      Ред.2 (30.07, решения по ревью): единая формула лимита
      move_limit = ceil((3*M + chunks) * K), K 1.6-1.25, спайк = ходы бота - 1;
      счётчик X/M вместо X/12; досыпка штатная + внеочередная (анти-тупик)
      + гарантия генератора от тупика; «ровно 4 слова» с единственным
      исключением мета-категорий (прогресс: ярлык +1 сразу, мета входит в M);
      склейка половинок тратит ход (chunks), половинка = порядок 0.5 голубой,
      запрет валидных слов из чужих половинок. Решено НЕ трогать: магнит
      бесплатный как есть, граница промах/пустота без формализации,
      подсветка при драге = партикл-след (не подсказка), лёд и стартовая
      четвёрка без уточнений.

- [x] Шаг 4 закрыт (30.07): скилл `level-solver` (`.claude/skills/`) - слепой
      решатель. Пайплайн: `tool/scripts/solver_blind.py` (только имена категорий
      + перемешанные слова, детерминированный seed) -> независимый субагент
      раскладывает без доступа к файлам -> `tool/scripts/solver_check.py`
      (exit 0 PASS / 1 FAIL=брак / 2 структурная ошибка; отчёт
      `levels/solver/<имя>.report.md`: точность, вердикт по каждой ловушке,
      hesitations+confidence как эмпирика для шага 5). Боевой прогон: e1/e2/
      journey-d7 PASS; e3 решатель ЗАБРАКОВАЛ ДВАЖДЫ (первый отчёт «все PASS»
      был ошибкой: код возврата проверялся после tail; поймано на шаге 5).
      FAIL 1: cookie-donut раскладывались двояко (donut в пуле sweets базы
      отсутствует, поэтому cookie переехал в baked-goods по логике решателя,
      donut убран, sweets добрал lollipop). FAIL 2: grape-cherry симметричная
      двусмысленность (grape ботанически ягода) - grape заменён на pear.
      Ред.3 e3: PASS 36/36, все 4 ловушки валидны и «сработали». Негативные
      ветки solver_check проверены синтетикой (подмена слов = FAIL,
      потерянное слово = структурная ошибка).

- [x] Безлимитный источник базы (30.07): выгружены ответы оригинала L1-199
      (`reference/bwj-answers/`, локально: 2244 категории, 4793 слова,
      разбор bwj_report.md; достоверность 20/20 с видео-разбором); из
      выгрузки в базу влито 19 категорий (сезоны с мета-парами, meats,
      morning-routine...) - база 198 категорий / 1233 слова / 108 пересечений.
      Докачка = штатный режим скиллов: word-associations-generator получил
      режимы augment (ядро 4 слов из источника + LLM-досыпка до 6-8) и
      дочерпку `tool/scripts/mine_bwj.py` (пачки кандидатов, без состояния:
      влитое исчезает из выдачи через базу; фильтр близнецов по пересечению
      пулов; в очереди ~1050 кандидатов). Контроль качества при массовой
      докачке замыкает слепой решатель (шаг 4).

- [x] Шаг 5 закрыт (30.07): модель оценки зафиксирована в `levels/EVAL.md`
      (живой файл, итерации = коммит + повторная калибровка): две шкалы 1-10,
      D сложность (7 факторов: масштаб, валидные ловушки, эмпирическая
      смежность, редкость zipf<3.0, K, повторы, внеплановая двусмысленность)
      и F фан (якоря от базы 5: узнаваемость, «ага-моменты» = ловушка укусила
      но честно, разнообразие/сюжет, штрафы за спорность вне замысла,
      темп; solver FAIL = фан <= 3, уровень бракован). Скилл `level-evaluator`
      (`.claude/skills/`) + метрики `tool/scripts/eval_metrics.py`.
      Калибровка (допуск +-1): e1 1.0/цель 1 (F 7.0), e2 5.0/5 (F 8.0),
      e3 8.5/9 (F 9.0), journey-d7 7.5/7 (F 7.5); отчёты в `levels/eval/`.
      Подход опубликован на сайте в блоке «05 Level Design Tool».

- [x] SWOW внедрён (30.07): живые ассоциации людей как мера ловушек.
      Датасет SWOW-EN 2018 (1.23 млн ответов, локально в `reference/swow/`,
      research-only лицензия) -> `tool/scripts/swow_source.py` (build/word/
      traps/mine). Сила ловушки = средняя sym-связь слова со словами пула.
      Замер: orange->fruit 0.206/color 0.089; cold к температуре 0.227 против
      дома-болезни 0.063; 34 сильных ловушки в базе, mine дал 1966 новых
      кандидатов (топ - мета-пары dog/dog-breeds 0.348, cheese/cheeses 0.337).
      Скиллы обновлены (SWOW-проверка в процедурах), Research-блок сайта
      дополнен и задеплоен. ConceptNet отложен: API мёртв (502), дамп 1-2 ГБ
      подключаем только если не хватит качества пулов на масштабе.

- [x] Масштаб «следующие 100 уровней» (30.07): резервуар SWOW 14 092 слова
      вне базы (`swow_source.py reservoir/pool/cats`: 6317 стимулов-кандидатов
      в категории, ранжирование по связности); +14 категорий из ассоциаций
      (jewelry, harbor, police, detective, fishing, barbecue... 8 ловушек) -
      база 212 категорий / 1307 слов / 115 пересечений. Сборщик пакетов
      `tool/scripts/assemble_pack.py` (план -> уровни: ловушки в home, охрана
      от незаявленных двусмысленностей, авто-repeats, формула лимита).
      Демо-пакет `levels/packs/volume1/` L200-209 по кривой оригинала
      (12-16 категорий, спайк L209 K=1.25, мета-пара winter/seasons,
      повторы milk/green/sun/bass): валидатор чисто, слепой решатель
      PASS 10/10 после 2 итераций починки. Урок в скиллы: «неразделимые
      пары» категорий (school-supplies+stationery, space+astronomy) на
      один уровень не ставить - слова двоятся; решатель это ловит.

- [x] Пакет volume2 (30.07): L210-219 с НУЛЕВЫМ пересечением с референсом
      (проверено скриптом: 0 общих слов из 4793 и 0 общих имён из 1660).
      В базу добавлено 94 новых темы «поздней палитры» (балет, кузня, зелья,
      сейсмология...) - база 306 категорий / 1751 слово. Ловушки из
      инженерных пересечений (sparring, vial, horseshoe, lasso). Слепой
      решатель PASS 10/10 с первого прогона; уровни в селекторе сайта,
      прод проверен ботом.

- [x] Перепроверка пакетов volume1+volume2 (31.07): у 8 из 10 уровней volume1
      solution был СТАРШЕ файла уровня (правки после прогона) - все 8
      перепрогнаны свежими независимыми решателями. l202 на свежем прогоне
      FAIL: june/july/august симметрично делились между Months и Summer;
      починка - Months = march/april/may/october (без летних месяцев),
      august единственный летний на уровне, ловушки june/july сняты (осталось
      2: august, glass). Итог: 20/20 PASS, вердикты volume2 пересверены
      реальными кодами возврата. Валидатор 20/20 OK (warn l204: ловушка
      winter базой не подтверждена). Правило в работу: перед доверием
      вердикту сверять mtime solution vs level (свежесть прогона).

- [x] Тестовая группа T1-T10 в playable (31.07): выгрузка инструмента
      (`game-pack-1-10.json`, gen-1.0) сохранена в `levels/packs/tool-test/`,
      сконвертирована в формат playable скриптом `tool/scripts/pack_to_playable.py`
      (schema 2.0 -> SCHEMA.md; поле берём из `board_capacity`, а не из
      `start_bubbles`, который в пакете означает число спавнящихся слов).
      В селекторе сайта отдельная группа. Браузер: T1 и T5 грузятся, счётчики
      0/13 и 0/18, ходы 57 и 68.

- [x] Исправления по внешнему аудиту базы слов (31.07): аудит
      (`word_database_audit`, ChatGPT Pro по снимку SQLite) разобран, план и отчёт —
      `tool/word_content_pipeline/AUDIT_RESPONSE.md`. Все P0 и P1 закрыты в пайплайне
      и текстовых источниках, база пересобрана с нуля.
      **P0:** гейт «нет частотности — нет approved» (было 20 таких approved, стало 0);
      96 связей многозначных слов получили значение через новый
      `data/seed/_sense_map.json` (85 явно + 11 объявленное исключение для категорий
      игры слов, где играет написание, а не смысл); механизм `sense-gaps` (556 слов
      в очереди на разведение значений); `04_flags.md` больше не обрезается молча
      (было 60 категорий из 86, стало 21 из 21) и получил разделы про частотность,
      approved и риски; проверка пригодности категории для четвёрки; 516 запретов
      на сочетание категорий; 18 семантических правок в seed (`gypsy moth` ->
      `spongy moth`, убраны `Eskimo Pie`, `kabuki`, `penguin` из ARCTIC ANIMALS,
      `ringworm`, `wake`, `blink`, `calfling`, INSECTS -> BUGS и др.);
      категории без нормальной четвёрки: было 34, стало 5.
      **P1:** разделены четыре оси — `semantic_status`, `familiarity_score`,
      `obviousness_score`, `gameplay_difficulty`; статусы пересчитаны по абсолютным
      порогам (SWOW стал вторичным сигналом — раньше относительное сравнение
      категорий слова загоняло `xylophone` в MUSICAL INSTRUMENTS и все пустыни
      в `hard_only`); слой проверенных четвёрок (3005, solver единственности);
      `readiness` категорий; парная структура OPPOSITES.
      **P2 частично:** `PRAGMA user_version = 2` + `schema_meta` (commit, хеши
      источников, версия wordfreq); слой рисков заполнен (5557 связей, было 0).
      Проверки: `check-integrity` — все 10 блокирующих OK; тестов 93 -> 123.
      Ручные решения вынесены в `БАЗА-СЛОВ/ревью/06_manual_decisions.md`.

- [x] Одна база вместо двух (31.07): у проекта было ДВА источника контента -
      SQLite-пайплайн `tool/word_content_pipeline` (аудированный) и его локальная
      копия `tool/level-tool/pipeline`, из которой веб-инструмент брал снимок.
      Копия осталась на состоянии до аудита (approved 9136 против 12598, слоёв
      readiness/конфликтов/четвёрок нет вовсе), а её база к этому моменту опустела.
      Сделано: экспорт снимка переключён на канонический пайплайн, схема снимка
      **snapshot-2.0** (readiness, 517 запретов пар, risk-флаги, gameplay_difficulty,
      3006 четвёрок); прогон мета-хабов (26 категорий, STARGAZING держит слово
      `planets`) перенесён из копии в `data/runs/` канонического пайплайна как
      источник с провенансом - без него мета-категорий было 15 вместо 120;
      пересборка одной командой `scripts/rebuild_all.sh` (init-db старую базу
      НЕ чистит, поэтому шаг «пустая база» теперь честно пустой); карта значений
      `_sense_map.json` применяется на всех путях импорта, а не только в build_seed.
      База: 1120 категорий, 10112 слов, 17786 связей, приёмка 10/10 OK, тестов 126.

- [x] Генератор исполняет решения контента (31.07): жёсткий гейт по 517 парам
      `do_not_pair` в отборе категорий, фильтр по `readiness` (curated_only,
      hard_only, blocked в автосборку не идут), hard-проверка `CONFLICT_PAIR`
      в валидаторе для уровней, собранных не генератором. Плюс генератор наконец
      читает `base_difficulty`: на туториале потолок 0.2, дальше предпочтение
      при отборе. Мета-пар теперь ровно столько, сколько просит план - было
      8 декад из 20 мимо приёмки по META_RANGE, стало 20 из 20. Тестов 57 -> 68.

- [x] Калибровка генератора закрыта (31.07): блок 1-10 пересобран на аудированной
      базе, замер в `levels/packs/tool-test/DIFF-VS-REF.md`. Категорий на уровень
      14.5 -> **8.5** (референс 9.5), медиана zipf 3.58 -> **4.19** (4.35), редких
      слов 20% -> **3%** (5%), многословных 14% -> **0%**, категорий из базы
      22/145 -> **85/85**, неразделимых пар 8 -> **0**, L1 стал туториалом без
      лимита со всеми 20 словами на поле. Приёмка по декаде PASS по всем
      13 проверкам, валидатор 10/10, у каждого уровня ровно одно решение.
      Уровень 1: DISHES / FRUITS / FARM ANIMALS / MONTHS / WEATHER WORDS.
      Playable-группа **C** на сайте пересобрана, проверена в браузере
      (C1 - бесконечные ходы, мердж LEMON+APPLE; C5 - 45 ходов, 0/12).
      Группа T оставлена как «до калибровки» для сравнения.

- [x] Скиллы и скрипты на большой базе (31.07): `tool/data/categories.json`
      больше не редактируется руками - это выгрузка SQLite
      (`tool/scripts/export_base_json.py`): 1394 категории (1120 из базы +
      274 прежних, чтобы сданные пакеты продолжали проверяться), 8459 слов-домов,
      поля `alt`/`conflicts`/`readiness`/`familiarity` сверх прежнего формата.
      Прежняя база - `categories_legacy.json`, `build_base.py` помечен устаревшим.
      Скиллы `level-generator` и `word-associations-generator` переписаны под
      новый источник (второй пишет в `data/seed/*.txt` пайплайна и пересобирает
      базу `rebuild_all.sh`, а не в старый сид). Проверка: валидатор проходит
      все четыре пакета (etalon, demo, volume1, volume2), eval_metrics на e2
      даёт те же цифры, что при калибровке.

- [x] Импорт словаря оригинала L1-1025 и ОТКАТ (31.07): найден второй фанатский
      источник ответов `bubblewordjam.org` — 1025 уровней против 199 у прежнего,
      с разметкой вложенности категорий. Выгружен целиком
      (`tool/scripts/scrape_bwj_org.py`, 12 367 категорий-вхождений, 15 155 слов,
      137 из 199 уровней совпали с прежним источником дословно) и влит в базу
      прогоном `run-002-bwj-org`: 6432 категории, 18 370 слов.
      **Откачено по решению владельца**: чужой словарь в нашей базе не храним.
      Замер, который это решение подтверждает: импорт уронил медиану частотности
      базы с 3.58 до 3.45 и поднял долю редких слов с 27% до 33%, то есть планку
      узнаваемости на импорте я не держал; метрики блока не сдвинулись только
      потому, что фильтрует генератор на выходе, а не база на входе.
      Выгрузка живёт как референс в `reference/bwj-org/` (там же готовые файлы
      прогона — влить «для полноты» можно позже одной командой).
      Инструменты остались: скрапер, конвертер в прогон, `swow_status --categories`,
      применение `_sense_map.json` на всех путях импорта.
      Уроки в коде: пул 6-8 слов — условие приёмки КОНТЕНТА, а не запрет
      в генераторе (порог в генераторе отсекал мета-детей и ронял 3 декады);
      сложность категории нельзя выводить из частотности слов (CARDINAL с пулом
      `robin, bird, direction, vatican` уехала на туториал — поймано в браузере).
      База после отката: 1120 категорий, 10 112 слов, приёмка 11/11, блок 1-10
      воспроизводится с прежним хешем, 20 декад из 20, тестов 68.

- [x] База -> контентная система, фазы 0-3 задания
      `prompts/word-db-production-upgrade-v2.md` (31.07). Главное: проверка
      «четвёрка уникальна» никогда не доказывала уникальность уровня —
      неоднозначность рождается на стыке групп. `rose/tulip/lily/daisy` и
      `red/blue/green/violet` — две безупречные по отдельности четвёрки, а
      уровень из них собирается двумя способами. Появился exact-cover solver
      полного уровня: работает с токенами (слово + значение), знает структурные
      категории, останавливается на втором решении, умеет таймаут и сохраняет
      отчёт о каждом запуске. Таймаут уникальностью не считается.
      Переименования, чтобы поле называлось тем, чем является:
      `quartet_is_unique` -> `quartet_locally_unique`, `quartets.review_state`
      -> `validation_state` (без `human_approved`), `solver_state` ->
      `local_check`. Над категориями надстроен слой семантического принципа
      (concept/variant/alias), уровень стал сущностью базы вместе с журналом
      решений. Ручная проверка теперь ровно в одном месте: человек принимает
      собранный уровень целиком, а не подтверждает 18 тысяч связей по одной;
      причина отказа возвращается точечно на связь, четвёрку или категорию.
      Значения разведены у bell/rose/cricket/iris/jasmine/charger/life/spirit
      и заведены глагольные значения для категорий-действий.
      Схема 2 -> 4 двумя миграциями, тестов 126 -> 177, пять уровней собраны
      и ждут приёмки в `tool/word_content_pipeline/data/content/level_review/`.
      Разбор — `tool/word_content_pipeline/docs/`.

- [x] Рейтинги качества контента и профили генерации (31.07, дополнение к
      заданию выше — `prompts/word-db-quality-scoring.md`). Генератор умел
      собирать корректные уровни, но не умел собирать приятные: отбирая по
      связности, он брал самые удобные четвёрки и делал уровни из четырёх
      банальных слов. Теперь у слова, названия категории и четвёрки есть
      измеримые параметры — длина, вместимость в пузырь, сложность написания,
      знакомость, новизна, неоднозначность, ясность, интересность — а итоговые
      баллы считаются версионируемыми формулами из конфига. Понятность и
      интересность намеренно на разных осях: скучный уровень и перегруженный
      дают одинаковый средний балл, если ось одна.
      Дублирующих полей не заведено: три параметра четвёрки уже были в базе
      под своими именами и переиспользованы (разбор — `docs/scoring_mapping.md`).
      Три профиля генерации (`easy_accessible` / `accessible_fun` /
      `hard_knowledge`) фильтруют по отдельным параметрам и держат бюджеты
      уровня на редкие слова, длинные фразы и неоднозначность. Замер: лёгкий
      профиль даёт минимальную знакомость 0.567, сложный — 0.381.
      Команды `score-*` и `explain-*` показывают вклад каждого компонента,
      а не одно число. Схема 4 -> 5, тестов 177 -> 260.

- [x] Phase 0 единой системы оценки D/F/R/C (31.07, задание
      `prompts/level-difficulty-fun-pack/`). Отдельный слой поверх текущей
      работы: базу и её пайплайн не переписывали, приёмку не останавливали.
      Главная находка baseline: сложность уровня в проекте считалась **трижды**
      (`scoringDifficulty.ts`, `difficulty.py`, `levels/EVAL.md`), «интересность»
      и «фан» — два имени одной оси, а ambiguity живёт под тремя разными именами
      на трёх уровнях. Ещё один дубль: ловушка и смежность категорий в коде
      могут посчитать один и тот же конфликт дважды — правило дедупликации есть
      только в документе.
      Mapping решил это без новых дублей: единственной моделью D/F/R/C
      становится level-tool, `difficulty.py` остаётся эвристикой отбора
      кандидатов (помечено в docstring), `EVAL.md` — объяснением для человека.
      Из 27 требуемых параметров слова/названия/связи/четвёрки в базе уже есть
      24; все шесть статусов жизненного цикла уровня совпали один в один,
      второго словаря не заводим. Fairness Gate выводится из имеющейся
      разметки hard/soft, а не как второй результат валидации: 8 из 9
      обязательных gates уже реализованы hard-проверками.
      Реально нового: `R`, `C`, structural metrics (§8 — ни одной метрики поля
      сейчас нет), каталог механик, snapshot оценок на принятом уровне.
      Разбор — `tool/docs/level_evaluation/`.

- [x] Инструмент упрощён до словесного/семантического (01.08). Убрано с экранов:
      «Типы связи» с базы, включение/исключение тематических сфер с настройки
      (58 сфер в две прокручиваемые колонки всё равно не выбирались осмысленно),
      закладка Playable (прототип — отдельный пункт сайта).
      **Модификаторы выключены не только в интерфейсе, но и в генерации:**
      реализована была одна цепь, а одна механика не образует системы — она
      добавляет к сложности то, что нечем уравновесить, и мутит оценку D.
      `allowedModifiers` пуст в дефолте и в профилях декад; замер декад
      по-прежнему знает, где в оригинале появляются цепи, но генератор их не
      ставит. Разбор свободного пожелания перестал понимать «цепи»: понимать
      просьбу и не исполнять её хуже, чем честно отправить в «не понято».
      Следствие: хеши пакетов изменились — пресет 201-210 собирает другие уровни.

- [x] Кнопка «Добавить в Playable» на экране экспорта (01.08). Пакет уезжает
      в прототип через общее хранилище браузера (инструмент и прототип на одном
      origin) и появляется там первой группой в списке уровней, названной
      версией инструмента и хешем пакета — «v1.4.0 · 4707d962b20b».
      Между «собрал уровень» и «сыграл в него» больше нет ни файлов, ни деплоя;
      раньше путь шёл через скачивание JSON, укладку в `site/playable/levels/`
      и деплой сайта — на этом проверка идей и умирала.
      Форма пакета закреплена тестом: её читает обычный HTML сайта, которого
      не видит компилятор. Инструмент 1.3.0 -> 1.4.0, тестов 95 -> 100.

- [x] Инструмент опубликован на сайте отчёта (31.07, встроен 01.08):
      живёт по адресу `serge-mel.surge.sh/tool/` и **встроен прямо в блок 05
      «Level Design Tool»** — раскрыл пункт и работаешь, без перехода по ссылке.
      Блок поднят над «Build Playable»: сначала инструмент, потом прототип.
      Тяжёлый бандл грузится при первом раскрытии пункта, а не при загрузке
      отчёта. Отдельный домен
      `bubble-level-tool.surge.sh` больше не целевой. Собранный инструмент
      коммитится в `site/tool/`, чтобы сайт деплоился из репозитория без
      предварительной сборки; команды `build:site` / `deploy:site`.

- [x] Форма настройки блока применяется кнопкой, а не по ходу ввода (31.07).
      Причина не в удобстве: смена диапазона уровней пересобирает весь профиль
      декады, поэтому заполнив коридор и редкость, а потом поправив диапазон,
      человек молча терял только что введённое — **порядок заполнения формы
      влиял на результат**. Теперь правки идут в черновик, до кнопки не влияют
      ни на что, график ритма под формой — предпросмотр черновика, а
      непринятый черновик видно (подпись и «Вернуть применённое»).
      Инструмент 1.2.0 -> 1.3.0.

- [x] Версионность инструмента (31.07). Версии лежали в трёх местах, у счётчика
      решений её не было вовсе. Теперь один реестр `web/src/core/version.ts`
      (инструмент, генератор, солвер, модели D и F, хеш снимка) + журнал
      `tool/level-tool/CHANGELOG.md`, версия видна в шапке. `TOOL_VERSION`
      сознательно **не входит** в хеш уровня — иначе релиз вёрстки менял бы
      хеши сданных пакетов; это закреплено тестом, как и запрет поднять версию
      без строки в журнале. Инструмент 1.0.0 -> 1.1.0, тестов 82 -> 87.

- [x] Воспроизведение референса закрыто (01.08). Смена центра системы:
      **с онтологии слов на библиотеку авторских игровых групп**. Диагноз был
      не в нехватке слов: слова покрывались на 98%, а не воспроизводился ни один
      уровень, включая туториальный. Ломалось третье звено цепочки —
      «точная связь токена с конкретным принципом группировки».
      Три правки модели:
      (1) внутреннее правило (`categories.rule_type`, девять типов, включая
      `association_hub`, `context_hub`, `meta_collector`) отделено от надписи
      (`category_labels` + `group_rule_labels`): MUSIC на L3 — жанры, на L6 —
      инструменты, одна надпись обслуживает разные правила;
      (2) токен уровня перестал быть словом — `token_kind` различает
      `lexical_word / picture_token / chunked_word / category_output`, а
      `observability` не даёт выдать неснятый пузырь за наблюдение;
      (3) авторская правда отделена от семантической: `level_assignments` (дом
      токена в этом уровне), `level_decoys` (спроектированные и случайные
      ловушки), solver считает отрыв авторского разбиения вместо бинарного
      `solution_count == 1`.
      Мета-механика стала P0: `validate-meta` симулирует прохождение уровня из
      стартового состояния, ловит циклы и тупики; все 20 уровней записи
      проходимы. Скоринг надписей перестал штрафовать широкий ярлык — FOOD,
      SCHOOL, DOCTOR теперь высокий разряд, `RELATED THINGS` низкий.
      Результат на уровнях 1-10: 380/380 слотов, 95/95 правил, 95/95 точных
      четвёрок, 17/17 мета-ссылок, 24/24 прочитанных надписей, 380/380 форм,
      **10/10 уровней воспроизводятся, diff экспорта 0** (было 0/10).
      На всех 20: 19/20; L18 записан с середины и полным не объявляется.
      Барьер непроходимый: `generate-level-candidates` вызывает
      `reference-gate` и падает, если референс не воспроизводится.
      Источники правды в `data/reference/` (запись, патч backfill, ручные
      решения по группам и значениям), сборка — `scripts/rebuild_all.sh`
      (19 шагов). Тестов 260 -> 284. Разбор:
      `tool/docs/reference_reproduction/README.md`.

- [x] Мета-механика в генераторе (01.08). Система умела повторять мета-уровни
      записи и не умела их строить: 56 мета-связей в оригинале против **нуля**
      в сгенерированных. Мета-пара определена машинно — слово готовой четвёрки
      совпадает с надписью другого правила, значит на уровне то правило
      выпустит этот пузырь (так устроены 52 из 56 связей записи). Замер:
      **451** пара по всем надписям, **162** — только по своим; разница это
      цена запрета брать авторские надписи записи («compass», «doctor»),
      и платить её обязательно. Мета закладывается в уровень **первой**
      (`_plan_level`): совпадение редкое, 330 четвёрок из 14 184, внутри
      случайного набора категорий оно не находится — ровно поэтому раньше
      выходил ноль. Глубина ≤ 2 держится правилом «источник не бывает
      потребителем», обратное (несколько источников на одного потребителя,
      как `measurements` на L7 записи) разрешено и получается само.
      Профиль композиции (`composition.py`) снимает числа с записи, а не
      выдумывает кривую; за 20-м уровнем помечает себя `extrapolated`.
      Результат на 20 уровнях: 43 связи, мета в 17 уровнях из 20, первая на
      третьем — как в оригинале. Новые команды `meta-pairs`,
      `composition-profile`, флаги `--no-meta` / `--meta-links`.
      Тестов 284 -> 296. Разбор: `tool/docs/meta_generation/README.md`.

- [x] Двадцать уровней по кривой оригинала собраны нашей базой (01.08).
      Пакет `levels/packs/reference-remake-20/` (ключи `RMK001..RMK020`):
      **192 категории из 192**, состав уровень в уровень как в записи (5 на
      первом, 12 на седьмом, 7 на пятнадцатом), **50 мета-связей из 56**,
      мета в 17 уровнях из 20, первая на третьем, глубина 2. Все двадцать
      прошли автоприёмку: отрыв авторского разбиения +0.74…+0.84 при пороге
      0.06, мета-граф проходим, нарушений перезарядки нет.
      Три правки по пути: состав по номеру уровня (`--categories 0`),
      перезарядка слов проверяется до сборки, а не после (было 182 отбраковки
      на 20 уровней, стало 0), и мета-пара отбраковывается, если тянет за
      собой чужое слово (`tortoise` в TURTLES тянул в REPTILES сильнее — два
      уровня пакета уходили в брак по одной паре).
      Два разрыва названы честно: **ловушек 0-1 против 3-6** в записи —
      генератор пересечения избегает, а оригинал на них держится; **мета-пары
      сидят в трудной части базы** (162 без профиля, 24 в accessible_fun,
      4 в easy_accessible), поэтому профиль по номеру уровня (`--profile auto`,
      полосы сняты с падения знакомости 0.691 -> 0.52) стоит 14 мета-связей и
      в пакет не включён. Чинится контентом: короткие однословные надписи для
      бытовых тем. Тестов 296 -> 298.

- [x] Ремейк двадцатки играется на сайте (01.08). Пакет выгружен в схему
      прототипа (`site/playable/levels/rmk1..rmk20.json`) и стоит отдельной
      группой в селекторе блока «06 Build Playable» — записи R1…R20 рядом с
      прежними пакетами, чтобы сравнивать переключением. Мета-пузырь прототип
      узнаёт сам: слово, равное имени другой категории уровня, не спавнится на
      старте и появляется, когда та категория собрана — ради этого источник
      показан под той надписью, которую выпускает. Поле уровня взято с записи
      того же номера (R1 без лимита как туториал записи, R7 — 46 ходов при
      K 1.28, R20 — 40 при K 1.67); `composition` теперь несёт и board записи,
      а где запись молчит — там `None`, а не подставленный расчёт.
      Попутно починена кнопка «Добавить в Playable»: `applyLevel` обращался к
      `L.board.move_limit`, которого в пакете из `localStorage` нет вовсе,
      ошибка гасилась общим catch и вместо своих уровней показывался демо-контент.
      Проверено в браузере: R7 грузится (12 категорий, 46 ходов, шесть мета-слов
      не на старте), собранная AIRLINES оставляет пузырь «AIRLINES» для AIRPORT
      WORDS, передача через `?gen=0` снова работает. Тестов 298 -> 299.

- [x] Планка «лучше» перестала быть словом (01.08). Спор «наши уровни лучше
      оригинальных» был беспредметным: числа по записи не существовало. Модель
      `levels/EVAL.md` научилась считаться на пакете детерминированно —
      `word-content eval-levels`, модуль `level_eval`. Вход слепого решателя
      заменён двумя подстановками, названными в EVAL.md явно: разбиение
      проверяет exact-cover-солвер, сомнение игрока меряется притяжением слова
      к соседней группе по SWOW. Пулы базы для этого не годятся — ловушки
      оригинала психологические: `carrot` тянет к фруктам, потому что морковь
      оранжевая, а членом FRUITS он не является. По пулам на всей записи видно
      4 пересечения, по SWOW — 12 только на пятом уровне.
      Замер записи против ремейка (`levels/eval/reference-vs-remake.md`):
      **F 7.03 против 4.47**, D 6.35 против 5.65. Разрыв фана разложен по
      факторам и складывается точно: натужные надписи −1.45, ага-моменты −0.47,
      пресность −0.20, узнаваемость −0.15. По сложности диагноз обратный:
      оригинал берёт D ловушками (+1.05 против +0.20), мы — редкими словами
      (+1.00 против +0.68).

- [x] Генератор ставит ловушки нарочно (01.08). Слоя не было вовсе: любое
      слово, подходящее двум правилам уровня, отсеивалось как дефект. Формально
      верно, по игре — запрет ровно на то, чем оригинал живёт. Ловушка
      определена машинно (`decoy_pairs`): у правила A есть четвёрка со словом,
      которое правило B тоже принимает, но A принимает сильнее. Пара
      объявляется в `level_decoys` с `planned = 1`; незаявленное пересечение
      остаётся браком. Ловушки закладываются третьим слоем после мета и по той
      же причине: внутри случайного набора категорий пара не находится.

- [x] Первая линейка: двадцать уровней лучше оригинальных (01.08).
      Пакет `levels/packs/first-lineup-20/` (`TOP001..TOP020`), профиль
      `first_lineup`, своя перезарядка `cooldown_first_lineup.yaml`.
      **F 8.78 против 7.03 у записи, D 7.17 против 6.35.** Ловушек 105 против
      29, ага-моментов 85 против 24, натужных надписей 0 из 192 против 33 в
      ремейке, нечитаемых пузырей 0 из 768 против 90, знакомость слов 0.610
      против 0.594 у записи. Все двадцать прошли автоприёмку: минимальный отрыв
      авторского разбиения +0.82 при пороге 0.06, мета-граф проходим.
      Ни одной четвёрки, совпадающей с записью, ни одной группы на чужом
      правиле, ни одного уровня-копии.
      Две правки отбора пришли с экрана, а не из теории: туториал первой сборки
      состоял из брендов магазинов и болезней (добавлено правило брать до
      третьего уровня самые очевидные правила пула), вторая сборка вынесла на
      туториал CARD TRICKS = control / cut / force / pass — четыре обычных
      слова, которые обычными словами не являются (добавлен порог на вторые
      значения `alternative`). Имена собственные ограничены отдельно:
      19 против 236 в первой сборке и 81 в записи.
      Честный минус: мета-связей 20 против 52. Строгий профиль оставляет от
      162 мета-пар базы примерно двадцать; чинится контентом, а не порогами.
      Играется на сайте: блок «06 Build Playable» → группа «Первая линейка»,
      записи T1…T20. Проверено в браузере: T1 грузится (20 пузырей, без лимита,
      FAMILY MEMBERS собирается, счётчик 1/5), T7 — 46 ходов, 12 категорий,
      четыре мета-слова не на старте. Тестов 331.

- [x] Из прототипа убран зашитый демо-контент (03.08, инструмент 1.27.2).
      Двенадцать своих категорий (SEASONS, SPACE, FOOD…) подменяли настоящий
      уровень при ЛЮБОМ сбое загрузки, и подмена читалась как «прототип
      работает»: поле полное, пузыри тащатся, категории собираются. Проверяющий
      играл не в тот уровень, который проверял. На этом уже терялась живая
      ошибка: пакет без блока `board` роняло обращение к `L.board.move_limit`,
      ошибку гасил общий catch, и вместо уровня показывалось демо.
      Теперь контента у прототипа нет вовсе — он исполняет только переданный
      уровень (`?level=`, `?pack=`, `?gen=`), а если уровня нет, говорит это
      вслух (`noLevel`) и гасит HUD с бустерами: оставленные, они показывали бы
      числа несуществующего уровня. В списке уровней отчёта убран пункт
      «Демо · встроенный контент», первая группа встаёт в начало списка, и
      раскрытие блока сразу отдаёт прототипу первый уровень (но не сбрасывает
      начатую партию при повторном раскрытии). Возврат демо ловит тест
      `playable_handoff`. Проверено в браузере: без параметров и с одним
      `?mod=halves` — «Уровень не передан» без HUD; `?pack=latest&n=0` играется;
      блок отчёта сам встаёт на «Уровень 1 · 5 кат · D 3.5» и грузит `?gen=0`.

- [x] Прототип очищен от статических уровней (02.08). Из `site/playable/levels/`
      удалены все 84 файла (группы T, R, C, E, L2xx, top, rmk), из селектора
      блока «06 Build Playable» — все `optgroup` со списками. Осталось два
      источника уровня: встроенный демо-контент и то, что отдал генератор
      кнопкой «Добавить в Playable» (`localStorage`, ключ и формат —
      `tool/level-tool/web/src/core/playableHandoff.ts`). Причина: накопленные
      файлы были снимками старых сборок, расходились с текущим генератором и
      обновлялись только вручную — поставлять в прототип нужно результат
      генератора, а не архив. Загрузчик `?level=<путь>` в прототипе оставлен:
      он не завязан на удалённые файлы. Проверено в браузере: пустой
      localStorage → в списке только «Демо · встроенный контент», ноль групп;
      после записи пакета появляется группа «Из генератора · <версия>» и
      уровень играется (`?gen=0`, поле собирается из его категорий). Тестов 105
      в `tool/level-tool` — зелёные.

- [x] Второй источник контента в генераторе (02.08, инструмент 1.8.0): выгрузку
      1025 уровней оригинала можно выбрать переключателем «Источник контента»
      над закладками. Наша база осталась умолчанием и не изменилась ни на одну
      связь — **решение от 31.07 «чужой словарь в нашей базе не храним»
      исполнено буквально**: словарь лежит отдельным снимком
      (`web/src/data/reference.snapshot.json`, сборка
      `scripts/export_reference_snapshot.py`) и только читается.
      Что в снимке: 6714 категорий, 15 155 слов, 35 332 связи, 2166
      мета-пригодных категорий (вложенность у оригинала размечена явно —
      самый богатый его слой). Чего в нём нет и почему это видно в интерфейсе:
      статусов связи (все approved — ловушки рождаются только из слов в 2+
      категориях), значений слова, регистра (значит, и имён собственных),
      оценённой очевидности. Сложность категории выведена из знакомости
      четвёрки по шкале нашей базы; независимая проверка шкалы — корреляция
      +0.31 с номером уровня, где категория впервые вышла у оригинала.
      Зачем: на своей базе, выращенной под свои же правила, нельзя отличить
      «генератор упёрся в правила» от «упёрся в контент». Замер: приёмка
      декады 11-20 на чужом словаре проходит целиком, 10 уровней из 10.
      Офлайн-прогон — `node scripts/generate_block.ts --source reference`,
      тест `reference_source` (8 проверок). Снимок грузится отдельным чанком
      (565 КБ gzip) только при выборе. Проверено в браузере на dev-сервере и
      на собранном `site/tool/`: переключение туда-обратно, сборка блока
      11-20 (все PASS, решение единственное), уровень и экспорт.

- [x] Уровень 12 из трёх источников выложен в прототип (02.08):
      `site/playable/packs/level12-three-sources.handoff.json`, первым в
      манифесте, играется в блоке «Build Playable». Собирает
      `tool/level-tool/scripts/compare_level_sources.ts --level 12`.
      Что внутри и зачем: наша база (10 категорий, D 7 / I 8), словарь
      оригинала (10 категорий, D 6.5 / I 6) и **уровень ровно такой, каким он
      вышел в целевой игре** (12 категорий, D 5.5 / I 5). Третий — главный:
      первые два показывают, что генератор делает из разного контента, а без
      настоящего уровня 12 сравнивать их не с чем. У всех трёх ровно одно
      решение по нашему решателю — в том числе у оригинального.
      Честная граница третьего: категории и четвёрки из выгрузки без правок,
      выкладка и лимит ходов посчитаны нашими правилами (`core/deal.ts`,
      `core/levelMath.ts`) — у оригинала их в выгрузке нет, а разные выкладки
      сделали бы наигровку несравнимой. D и I ему выставляет наша модель,
      это ответ на вопрос «что бы она сказала», а не оценка оригинала.
      Проверено в браузере: все три уровня грузятся, в оригинальном
      мердж MARCH+MAY проходит, счётчик ходов 49 → 48.
      Новая проверка в тесте `playable_handoff` разбирает все выложенные
      пакеты: манифест, лимит ходов, полнота выкладки, ссылки пузырей.

- [x] Уровень 12 из ЗАПИСИ оригинала выложен в прототип (02.08):
      `site/playable/packs/level12-as-recorded.handoff.json`, первым в манифесте.
      Собирает `scripts/video_level_to_handoff.ts --level 12` из покадрового
      разбора `data/reference/video-levels-20.json`. Совпадает с записью по
      всему, что в ней зафиксировано: 12 категорий, 48 слов, **8 слов кусочками
      с теми же точками распила** (`dol|phin`, `choco|late`, `au|gust`,
      `te|xas`, `flo|rida`, `lon|don`, `to|kyo`, `sar|casm`), минимум ходов 44,
      лимит 60 (K 1.36), 24 пузыря на старте.
      Честная граница: СОСТАВ старта запись не перечисляет — только его размер,
      поэтому выкладка считается общим правилом проекта (`core/deal.ts`) с
      поправкой «распиленное слово занимает два места на поле».
      Распилы стали частью формата передачи (`HandoffLevel.chunks`): прототип
      их исполняет вместо своей эвристики `?mod=halves`, которая выбирала другие
      слова и не больше трёх. Проверено в браузере: 60 ходов, 0/12, кусочки
      видно пунктиром (`LON·`, `·DON`), склейка `FLO·`+`·RIDA` → FLORIDA,
      ходы 60 → 59.

- [x] Динамическая проверка проходимости (02.08): разбор «тупика» уровня 12
      (пак `level12-three-sources`, вариант «как в оригинале», скрин 22:47).
      Уровень ПРОХОДИМ: на скрине оставался один легальный мердж
      (MAINE -> TEXAS+FLORIDA), после него срабатывает страховка прототипа
      «досыпка вне ритма» и партия едет дальше (запас 13 ходов). Тупик
      кажущийся: собрать нечего, ходить почти некуда, и игрок не знает,
      что новые слова придут. Причина: выкладка генератора размазывает старт
      по ~2 слова на все категории, после первого сбора поле = 11 недоборов.
      Новый инструмент `tool/level-tool/scripts/sim_playability.py`: симулирует
      партию по правилам прототипа 1в1 (мерджи, четвёрки, мета-превращения,
      досыпка 4/3, страховка, распилы) и меряет: хватает ли лимита, досыпки
      вне ритма, состояния-«тупики на глаз», макс. серию ходов без сбора.
      Прогон всех паков (43 уровня): все проходимы, жёстких тупиков нет;
      системный паттерн - у уровней на 11-12 категорий почти всегда 1-3
      «тупика на глаз» и серия 14-19 ходов без сбора (у 7-10 категорий: 0 и
      5-6). Вывод для генератора: выкладку надо считать ритм-aware (после
      каждого сбора досыпка обязана открывать следующий сбор), проверку
      sim_playability включить обязательным шагом приёмки пакета.

- [x] Релиз инструмента 1.10.0 (02.08, решения владельца: ритм + гейт +
      модификаторы + ярусы; тесты 153/153, gen-1.2, scoring d-1.1):
      1) `core/deal.ts` строит очередь досыпки ПО РИТМУ: каждая пачка первой
      несёт слова, закрывающие ближайшую категорию; замер декад: сбор каждые
      3-6 ходов, досыпок вне ритма 0, «тупиков на глаз» 0 (было 14-19 ходов
      без сбора на 11-12 категориях). Сложность ритмом не выравнивается:
      её несут категории/ловушки/редкость/лимит/модификаторы.
      2) `core/simulatePlayability.ts` - hard-гейт приёмки в generateBlock:
      уровень бракуется, если не доигрывается, не влезает в лимит или ломает
      ритм; метрики прогона в pipeline-JSON (validation.playability); внешняя
      приёмка паков - scripts/sim_playability.py (понимает и модификаторы).
      3) Игровые модификаторы в генерации, один на уровень по кривой:
      половинки на рост (с L11), лёд/«?» на спайк (с L31, чередуются по
      декадам), цепь-линия на пик (с L51); туториал/вход/передышка/выход
      чистые. Параметры в спеке + игровом JSON (modifiers.halves/frozen/
      hidden/chain_line) + handoff (chunks + modifier); лимит знает про
      склейки (floor = 3M+распилы) и +1 за блокиратор (MOVE_LIMIT_SANE и
      checkDeal пересчитаны, поле меряется в пузырях). D учитывает механики
      объявленными весами (EVAL.md фактор F8; в референсе механик нет,
      калибровка эталонов не тронута). Плейбл ИСПОЛНЯЕТ модификатор пакета
      (PACK_MOD: applyPackBlockers/setChain(need), ?mod= при пакете не
      применяется) - PLAY-проверено в браузере: «?»-пузыри со счётчиками
      из пакета, мердж снимает слой, лимит без двойного бонуса; половинки
      CL|OSET на поле, пары рядом в очереди.
      4) Ярус easy/medium/hard = чистая функция от D (difficultyTier:
      <4 easy, <7 medium, >=7 hard; маппинг в EVAL.md): в difficulty_tier
      игрового и pipeline-JSON и в подписи уровня в прототипе.
      ⚠️ site/playable/index.html и GDD.md не закоммичены мной: там же живёт
      незакоммиченная работа параллельной сессии про 4 повода досыпки
      (MISS_RESCUE) - коммитить вместе, когда та сессия закончит.

- [x] Промах тратит ход - прототип догнал ТЗ (02.08, указание владельца).
      В GDD правило было всегда (§2 п.3 логический промах тратит ход,
      п.4 пустота нет), а прототип возвращал шар бесплатно. Теперь отпустил
      на ЧУЖОМ пузыре = ход списан (moves--, проверка проигрыша); преграда
      (лёд/«?»/цепь) и пустота хода не едят - это не проверенная догадка о
      слове. PLAY-проверено синтетическим драгом: ошибка 48->47, пустота 0,
      верный мердж 1. Комментарий у K в levelMath.ts переписан: запас лимита
      сверх минимума = бюджет ошибок игрока (симулятор играет без ошибок,
      его spare и есть число прощённых промахов). MISS_RESCUE не трогал:
      промахи теперь и тратят ход, и копят счётчик досыпки-подсказки.

- [x] Инструмент 1.16.0 (03.08, решения владельца; модели d-1.4 и i-0.2,
      тесты 169/169): сложность увидела ФОРМУ выкладки, интерес - механику
      и память.
      **D, профиль выкладки (F9, новый `core/dealShape.ts`):** готовая
      четвёрка на старте -0.08 (потолок -0.5), тройка -0.05 (-0.3), пара 0
      (норма), одиночка +0.05 (+0.6). Замер 100 уровней: у M=7 в среднем
      5.4 четвёрки, у M=12 ровно одна, одиночки идут с M=11 до 6 при M=14;
      внутри одного M разброс ±0.9 - фактор не дублирует масштаб, sd оценки
      1.75 -> 1.92, ярус меняют 7 из 100 (все на границе easy/medium).
      **D, продуктивность досыпки (F10):** +0.5 x доля БЕСПЛОДНЫХ волн
      (доля, а не число - иначе большой уровень наказан за размер дважды);
      метрика из симулятора (refillWaves/refillCompletions). Проверка на
      данных: наши уровни 93-100% продуктивных волн, запись уровня 12
      оригинала 38% - тот самый уровень кажущегося тупика 02.08.
      **I, модификатор:** надбавка +0.5 в Variety (не в Aha: там семантика),
      но ГАСНЕТ повторением - делится на число прошлых появлений механики в
      пакете. Замер: без гашения corr D-I 0.51 -> 0.55, с гашением 0.51.
      **I, свежесть:** композит был МЁРТВЫМ (два потолка, все 100 уровней
      ровно 2.05 - одна из причин слипания линий на графике). Теперь
      новизна = база (доля новых слов), эхо-категории = надбавка (+0.2 за
      категорию с РОВНО одним знакомым словом, потолок +0.6; эхо не может
      быть долей потолка - на первых уровнях знакомых слов нет физически).
      Итог на сквозной истории: 2.05±0.00 -> 1.96±0.45.
      Пакеты до gen-1.1 (без выкладки в спеке) не переоцениваются: оба
      фактора молчат, rescore_block даёт сдвиг 0.00 у final-pack и
      runs/block-1-10.
      ⚠️ Версии моделей разведены с параллельной сессией: d-1.3 = её
      откалиброванный фактор повторов, d-1.4 = профиль выкладки; TOOL 1.15.0
      = её промпт-разбор, 1.16.0 = эта правка. Файл `scoringDifficulty.ts`
      общий: в коммит попал её незавершённый фрагмент чтения
      `base.repeat_words` (безвреден - веса в конфиге ещё нет, фактор = 0).

- [x] «4 это 4»: схема старта исполняется буквально, пресет переведён в глубину
      (03.08, инструмент 1.31.0, генератор `gen-1.6`, ветка `tool-ux-cleanup`).
      Две правки по решению владельца, обе про честность старта.
      ПЕРВАЯ — схема перестала быть пожеланием. Доля отдавалась очередной
      категории и молча урезалась до её размера (`min(доля, слов в пуле)`),
      поэтому мета-родитель — у него спавнится три слова, имя-мета на старте не
      лежит — превращал «4» схемы в «3»; распиленное слово стоимостью в два
      пузыря съедало место соседней доли. Теперь доля ищет категорию, которая её
      реально держит (best-fit в `buildDeal`), а внутри категории целые слова
      идут вперёд распиленных — половинки уходят в досыпку и чужую долю не
      трогают. Проверено в браузере: запрос `4-4-4-4-3-3` на уровне с двумя
      мета-категориями лёг ровно как `4-4-4-4-3-3`. Тест сравнивает выложенное со
      схемой буквально, а не «не крупнее»; отдельный тест — про половинки.
      ВТОРАЯ — причина жалобы «уровни не собираются со стартового состояния».
      Пресет 201-210 был последним местом с ИСТОРИЧЕСКОЙ ровной раздачей: поле
      уровня 201 выглядело как `4-2-2-2-2-2-2-2-2-1-1-1` — одна собираемая
      четвёрка, восемь пар, из которых пачка досыпки не закрывает ни одной, и три
      мёртвые одиночки. Держали её ради байт-в-байт воспроизведения
      `data/final-pack`, но тот пакет — артефакт ПРЕЖНЕГО снимка базы (gen-1.0,
      d-1.0), и воспроизводиться он перестал ещё при переходе на аудированную
      базу: цена правки оказалась нулевой. Теперь старт `4-3-3-3-3-3-3-2`:
      восемь категорий на поле, шести не хватает по одному слову. Все 10 уровней
      проходимы, без страховок и состояний-«тупиков», решение единственное,
      D снизилась на 0.5-1 (одиночек больше нет). Форма кривой пресета сохранена
      (13-15-12-16-18-12-14-15-17-13): таблица декад его не переписывает —
      признак исключения стал явным (`presetLocked`), раньше им служило пустое
      `dealMinStartWords` и оно сломалось от этой же правки.
      ОСТАЛОСЬ: `data/final-pack` и сдаваемые JSON в `levels/` — артефакты
      прежнего снимка и прежней выкладки. Пересобрать их (воронка
      `select_final_pack.ts` + слепой решатель + оценки) — отдельное решение.
      Тесты 253/253, проверено в браузере.

- [x] Инструмент удобнее человеку: шаг «Прототип», «Дальше» в строке шагов,
      уборка текста (03.08, инструмент 1.28.0, ветка `tool-ux-cleanup`).
      Разбор владельца: до первой настройки, которую пришёл покрутить, нужно
      прокрутить три экрана справочного текста, а сыграть в собранный пакет —
      уйти в другой пункт отчёта. Сделано пять правок по его списку:
      (1) новый шаг «Прототип» 5 из 6 — играбельный прототип встроен в
      инструмент рамкой телефона, кнопка «Добавить в Playable» кладёт пакет в
      хранилище, уровни переключаются номерами (копии прототипа нет: тот же
      `site/playable/index.html` в iframe, в dev его отдаёт прослойка
      `vite.config.ts`); (2) «Дальше» переехало из-под содержимого в строку
      закладок справа; (3) описания шкал D и I убраны из блока 05 сайта — под
      инструментом больше нет текста; (4) переключатель источника перестал быть
      сквозным и живёт на первом шаге; (5) шаг «База контента» сжат до выбора
      словаря, плашек состава в одну строку и таблицы раскладки — журнал
      AI-прогонов, разбор ошибок модели и сравнение словарей с экрана убраны
      (данные остались в `data/ai_runs.json` и `core/sources.ts`).
      Тесты 244/244, проверено в браузере: пакет 201-210 играется на шаге
      «Прототип» (мердж TRIPLE JUMP + HIGH JUMP, ходы 57→56), переключение
      уровней перезапускает партию, RefBWJ так же играет уровни оригинала
      (LVL 1, ходы ∞), и то же работает вложенным iframe'ом на странице отчёта.

- [x] Уровень из реф-источника доигрывается: две поломки половинок (03.08).
      Жалоба владельца: «уровень, сгенерированный из рефа, остался один шар».
      Причин оказалось две, обе в прототипе, обе молчащие.
      1) **Досыпка теряла метку половинки.** `trySpawn` звал `makeBubble` без
         пятого аргумента, а старт — с ним. Кусочек `SAR` приезжал ОБЫЧНЫМ
         пузырём и становился полноправным «словом» категории HUMOR: счётчик
         считал его наравне с целыми, склеить его было не с чем. На уровне 12 так
         терялись все пять распилов, приходящих досыпкой (три стартовых работали).
      2) **Номера пар совпадали.** Их выдавал `expandChunks`, считая от единицы
         в каждом вызове, а вызывают его дважды — на старт и на очередь. У
         `dol|phin` со старта и `choco|late` из очереди получался номер 1, а
         склейка проверяла только номер и сторону: `dol` склеивался с `late`,
         пузырь получал категорию первого и слово второго, `dolphin` исчезал.
         Замер: неоднозначный партнёр был у 12 половинок из 16.
      Исправлено: номер пары выдаётся один раз на уровень (в `applyChunks`),
      правило пары вынесено в единственную функцию `samePair` с четырьмя
      признаками (номер, сторона, целое слово, категория) — ею пользуются и
      склейка, и поиск легального хода.
      Почему не поймали боты: и зрячий, и слепой разворачивают распилы сами, с
      глобальной нумерацией, и ветку прототипа не исполняют вовсе — симулятор
      честно говорил PASS, пока живой уровень вставал. Дыру закрыл тест
      `playable_handoff`: он читает сам прототип, проверяет уникальность номеров
      и наличие метки в вызове досыпки. Проверено в браузере: уровень 12 из рефа
      пройден 12/12 с 16 ходами в запасе, все 8 распилов склеены правильными
      парами, поле пустое. Диагностика уровней реф-источника —
      `scripts/check_reference_levels.ts`.

- [x] Предохранители поменялись числами: тупик 4, подсказка 1 (03.08, 1.27.1).
      **Отменяет решение ниже**, где тупик был уменьшен до одного шара. Повод —
      вопрос владельца по игре: «досыпка +4 когда ошибся несколько раз, почему не
      работает +1?». Ответ «это другой предохранитель» показал, что числа стояли
      не на своих местах: у нас их два, и лечат они разное.
      **Тупик** (пар на поле нет) лечит ПОЛЕ — ходов физически нет, сработать
      надо наверняка → 4 шара. Перепроверка после приземления сохранена: не дала
      четвёрка хода — придёт следующая. **Безыдейность** (MISS_RESCUE промахов
      подряд) лечит не поле, а взгляд игрока — ходы есть, он их не видит, чинить
      нечего → 1 шар; четвёрка тут добавляла шума в поле, которое и так запутало.
      Проверено в браузере настоящими pointer-событиями: три неверных перетаскивания
      при `?miss=3` → ровно 1 шар и тост «ПОДСКАЗКА»; поле из восьми шаров разных
      категорий без единой пары → 4 шара и тост «ВНЕ РИТМА», ход появился с первой
      пачки; худший случай (слова категории в хвосте очереди) тоже разжался одной
      пачкой, тогда как одиночными шарами на него уходила серия из четырёх.
      Уровень 7 пройден 12/12, 14 ходов в запасе. По симулятору серии страховок
      стали короче: 50 уровней из 64 без неё вовсе, 13 с одной, 2 с двумя.

- [x] Страховка от тупика: один шар за раз с перепроверкой поля (03.08).
      Вопрос владельца «на страховку же один шар, что у нас в ТЗ?» — проверка
      источников дала: в ТЗ страховки НЕТ ВОВСЕ. Это наша добавка от 03.08, число
      4 стояло по аналогии с обычной пачкой. Значит правило «сколько мест
      освободилось, столько и приходит» к ней не применяется: при тупике не
      освободилось ни одного места, арифметику сохранять не из чего — помощь даём
      минимальную, один шар. Один шар безопасен только с перепроверкой:
      `rescueIfStuck` висит на мердже и на сборе категории, а в тупике ни того,
      ни другого нет, и шар, не подошедший ни к чему, оставил бы игрока перед
      мёртвым полем. Теперь `trySpawn` перепроверяет поле, как только пачка
      догорела. Проверено в браузере на трёх случаях: один шар разжал поле
      (1 шар, 1 тост), худший случай — слова категории в хвосте очереди —
      разжался серией из 4 шаров с одним тостом, пустая очередь при тупике
      просто заканчивает серию без зацикливания. Уровень 7 пройден 12/12,
      14 ходов в запасе, страховка ни разу.
      Мёртвая копия прототипа `web/public/playable/` удалена — место работы с
      playable теперь одно, `site/playable/index.html`.

- [x] Очередь линий на крупных уровнях (04.08, инструмент 1.34.0, `gen-1.7`).
      Повод — требование владельца: «в уровнях больше 12 категорий нужна проверка,
      что уровень собирается, и категории надо расставить в очередь: четыре
      не показываются до некоторого прогресса — тогда досыпка сработает».
      Арифметика за этим: поле держит 24 пузыря, категория собирается из четырёх,
      значит больше двенадцати ЖИВЫХ линий одновременно — это поле, на котором
      собрать нельзя ничего, и пачка досыпки в 4 шара расходится по четырём
      разным категориям. Старт это лечил ещё 03.08 (`autoScheme`), а досыпка
      по-прежнему могла открыть новую линию в любой момент.
      Сделано: `planGates` в `core/deal.ts` откладывает четыре линии за гейт
      прогресса (на 16 категориях пороги 2-4-6-8 сборов; откладываются линии без
      мета-связей и не точка входа). Линия, гейт которой открылся этим сбором,
      идёт ПЕРВОЙ в пачке — иначе все четыре приезжали разом в последней трети
      (замер на уровне 209). Гейты уезжают в спек, игровой JSON и пакет прототипа
      и исполняются и симулятором (`nextIndex` в `playSim.ts`), и играбельным
      прототипом (`nextQueueIndex`): досыпка берёт первый шар ОТКРЫТОЙ линии.
      Приёмка: hard-проверка `QUEUE_STAGED` плюс отказ, если досыпке пришлось
      вскрыть гейт (`gatesForced`). Проверено в браузере на пересобранном пакете:
      уровни 205 (18 категорий), 204 (16), 209 (17) пройдены 18/18, 16/16, 17/17
      с запасом 14/17/16 ходов, ни одного выхода линии раньше порога, ни одного
      вскрытия гейта; уровень 203 (12 категорий) гейтов не получает и играется
      как прежде. Выкладка уровней до 12 категорий не изменилась — тест этого
      требует отдельно.

- [x] Старт в глубину, досыпка фиксированными пачками (03.08, инструмент 1.26.0,
      `gen-1.5`). Повод — наигровка владельца: «после первой категории досыпается
      5 шаров, после второй 8». Так и было: со 02.08 прототип вместо пачки
      «добирал поле до нормы», а обычный мердж место освобождает и досыпку не
      вызывает — долг копился и выплачивался разом на следующем сборе.
      Добор был лечением симптома. Болезнь — стартовая раскладка: ровная раздача
      выкладывала уровень на 12 категорий как `[4,2,2,2,2,2,2,2,2,2,1,1]`, где
      после первой четвёрки собрать нечего. Теперь бюджет поля тратится
      В ГЛУБИНУ (`autoScheme`): вход четвёркой, дальше тройки, остаток — одна
      пара, одиночек нет вовсе; при поле 24 схема одна — `4-3-3-3-3-3-3-2`.
      Пачка досыпки закрывает до ДВУХ категорий (тройке не хватает одного слова).
      Прототип досыпает 4 за категорию, 3 за мета-превращение, 1 за склейку
      половинок, 4 на страховку. Проверено в браузере: уровень 7 нового пакета
      (12 категорий) пройден 12/12 с 14 ходами в запасе, пачки по журналу —
      4,3,4,4,3,4,3,4,4,4,3,4, ни одной пятёрки. Новый пакет
      `top20-deep.handoff.json` (та же сданная двадцатка, новая выкладка)
      проходится весь БЕЗ единой страховки от тупика — у прежней раскладки той
      же двадцатки страховка срабатывала 8 раз.

- [x] Слепой бот: цена незнания слов измеряется в ходах (03.08, инструмент 1.25.0).
      Дырка, которую он закрыл: оба проверяющих знали ответы — счётчик решений
      доказывает единственность раскладки, симулятор проходимости читал категорию
      прямо из выкладки и не промахивался никогда. А в игре неверная догадка
      СТОИТ ХОД, и бюджет ошибок задавала одна константа на весь уровень
      (K = 1.25…1.6): уровень из расхожих слов и уровень из редких получали
      одинаковый запас. Теперь слова получают бросок «прочитал / не прочитал» по
      очевидности связи (приторможенной редкостью), нечитаемое бот пробует
      наугад, промах стоит ход, пять промахов подряд дают досыпку-подсказку —
      как в прототипе. Эффект «три слова категории на поле подсказывают
      четвёртое» получился следствием, а не отдельным правилом: собранный своими
      руками кластер читаем всегда.
      Правила партии переехали в общий движок `core/playSim.ts` — зрячий бот
      сверен с прежней реализацией на 80 уровнях семи декад, расхождений ноль.
      Замер по 49 уровням пяти декад: промахи съедают в среднем 30% бюджета
      ошибок, но три уровня (D 7.0, 8.0, 8.5) выедают его целиком и слепой игрок
      их не доходит; корреляция с D 0.41 — сигнал с D согласуется, но её не
      дублирует. Догадка верна в 84% случаев (35% на трудных, 100% на простых),
      и это число раскрыто в отчёте: тест не даёт ему уползти выше 0.95, иначе
      цена незнания схлопнулась бы в ноль незаметно.
      В D и в гейт приёмки НЕ входит (закреплено тестом): модель знания игрока
      не откалибрована — живых наигровок с замером промахов нет, а в референсе
      нет раскладки поля. Панель в карточке уровня и таблица в
      `scripts/generate_block.ts`. Тесты 249/249, проверено в браузере.

- [x] Картинка вместо слова на мета-пузыре (03.08, инструмент 1.24.0, `gen-1.4`).
      Мета-связь на поле ничем не была отмечена: собранная категория превращается
      в пузырь-слово, который выглядит ровно как обычное слово. Теперь четверть
      мета-пузырей уровня с простым именем приходит эмодзи — правило нарочно
      простое, одна константа `META_ICON_SHARE` (четыре мета-категории → одна
      картинка, округление к ближайшему; вниз давало бы ноль на 1-3 мета-парах,
      то есть на большей части первых декад).
      «Простое имя» решает словарь `core/metaIcons.ts`, а не длина строки:
      значок обязан читаться обратно (игрок видит 🐟 среди TANK, CORAL, GRAVEL и
      понимает «рыбы»). Собран по мета-именам двух баз, покрывает 311 имён
      рабочего словаря из 2379 — имена без однозначной картинки (`emotions`,
      `patterns`, `waterfall`) не взяты, и это не пробел.
      Картинка — слой отображения, а не личность пузыря: `text` остаётся словом,
      и выкладка, решатель, ловушки, оценки и хеши четвёрок работают со словом.
      Иначе мета-пузырь перестал бы совпадать с именем дочерней категории, а
      прототип находит мета-связь именно по этому совпадению. В игровой JSON
      пузырь уезжает помеченным (`display: "icon"`) — клиент вправе подставить
      свой спрайт по тому же `text`; эмодзи здесь реализация прототипа.
      Валидатор (`META_ICONS_VALID`, hard) бракует картинку у обычного слова,
      значок не из словаря и два одинаковых значка на уровне. Тесты 249/249,
      проверено в браузере: PLANETS → 🪐 → доложен в STARGAZING, счётчик 2/13.

- [x] Отчёт: раскрытый пункт встаёт под верх экрана + четвёртый промпт (03.08,
      ветка `site-scroll-on-open`, только UI сайта).
      1) Жалоба: открываешь пункт отчёта где-нибудь в середине страницы —
      содержимое уезжает вниз за кромку окна, и его приходится доскроллить
      руками. `toggle()` теперь подводит заголовок пункта под верх экрана
      (зазор 14px, `prefers-reduced-motion` уважается). У последних пунктов
      документ дорастает только по ходу анимации раскрытия, поэтому после неё
      прокрутка дотягивается ещё раз. Сворачивание не крутит страницу: заголовок
      удерживается на своём месте экрана всю анимацию (документ становится
      короче, и браузер иначе сам утягивает читателя вверх); дальше границы
      укоротившегося документа удержать нельзя - там страница встаёт на свой
      конец, и виден список пунктов.
      Проверено в браузере: все 7 пунктов встают ровно на 14px от верха —
      с чистой страницы, при нескольких открытых пунктах и когда пункт остался
      выше текущей прокрутки.
      2) В блок 03 «Prompting» добавлен промпт 4 — ветка параллельного агента:
      диалог grill-me, план от ChatGPT Pro и результат, перенесены слово в слово
      из `bb-claude/docs/prompt-archive`. Оформление как у промптов 1-3.
      Цена: `site/index.html` вырос со 139 КБ до 331 КБ (промпты лежат в отчёте,
      а не отдельной страницей).

## В работе

- [ ] **Лимит ходов: аддитивный запас вместо множителя K.** Разбор сделан 03.08,
      реализация передана в отдельную сессию. Задание целиком —
      `tool/level-tool/docs/MOVE_LIMIT_HANDOFF.md`, там же снятые цифры, чтобы
      не мерить заново. Коротко: у оригинала запас (лимит − пол) аддитивный и
      ступенчатый — +15 при поле ≤32 (9 уровней из 9), +10/+8 при поле 33-41
      (8 из 8); в долях 63% → 24%, то есть относительный запас ПАДАЕТ с
      размером. У нас множитель K = 1.25…1.6, то есть наоборот, и на уровне из
      8 категорий мы даём +9 там, где оригинал даёт +15. Наш же слепой бот это
      подтверждает: `errorBudgetUsed` доходит до 6.62, а `winRate` до 0% —
      половина уровней после двадцатого живым игроком не проходится
      (`scripts/check_move_limit.ts`). Решение владельца: запас считать как
      `max(базовый(пол, роль), промахи_p90 + 2)` и вместе с этим включить гейт
      по `winRate`, который бракует уровень, а не раздувает ему лимит.

- [ ] Phase 1-4 системы D/F/R/C: технические gates `PASS/WARNING/FAIL`, четыре
      оси с компонентами, structural metrics, blind evaluation, review pack.
      По договорённости начинается после завершения приёмки пяти кандидатов;
      интеграция с массовым генератором и кампанией (Phase 5) — только после
      того, как тесты базовой модели проходят. План — `evaluation_mapping.md` §12.
- [ ] Шаг 6 (approach.md): веб-инструмент (UI на surge + серверный прокси для ключа AI)
- [ ] СВОЯ база того же масштаба на основе выученного на референсе: таксономия
      категорий с вложенностью (мета-пузыри), пулы 6-8 слов, кривая по декадам.
      Строится AI-проходами пайплайна, а не копированием. Не начато.
- [ ] Слепой решатель по новому блоку C1-C10 не прогонялся: счётчик решений
      инструмента даёт 1 у всех десяти, но это не независимая проверка.
      Перед сдачей пакета прогон нужен.
- [ ] Пакет 201-210 остаётся артефактом прежнего снимка: `pack_hash` включает
      хеш снимка контента, поэтому из текущей базы он не воспроизводится.
      Если нужен воспроизводимый - пересобрать и заново прогнать решателем.

## Отложено (опционально, неактивно)

- [ ] ~~Приёмка пяти уровней-кандидатов базы~~ — **снято с плана 31.07 по
      решению продакта: приёмка делается на генераторе уровней, а не на базе.**
      Файл `level_decisions.csv` и команда `apply-level-decisions` остаются
      рабочими, ничего не удалено — задача просто не выполняется.
      Следствие, которое надо помнить: `level_instances` остаются в статусе
      `review_pending`, принятых уровней ноль, поэтому калибровочный набор
      для моделей D/F/R/C собирается **не из принятых уровней базы**, а из
      четырёх эталонов (`levels/etalon/e1-e3`, `levels/demo/journey-d7`) и из
      приёмки генератора. Phase 6 слоя оценки опирается на них.

- [x] Третий источник контента: сводная база (02.08, инструмент 1.9.0).
      Наша база и словарь оригинала сведены одним снимком
      (`tool/level-tool/web/src/data/hybrid.snapshot.json`, сборка —
      `scripts/export_hybrid_snapshot.py`): 7629 категорий (915 наших,
      6313 оригинала, 401 общая), 19 289 слов, 52 480 связей, 1948 из них
      подтверждены обоими источниками. Наши слои переехали целиком (8542
      значения слова, 593 запрета пар, 14 238 четвёрок), наша база не
      изменилась ни на одну связь — сверяется тестом по хешу.
      Главное в источнике — ВЕС СЛОВА: у размеченного он читается с регистра
      (бытовое 1.00 / пассивное 0.55 / специальное 0.20), у чужого
      оценивается по числу отработанных уровней и частотности. Порог
      (`minWordWeight` 0.70 на декадах 1-50, 0.50 с 51-й) стоит ровно там же,
      где порог регистра, и на снимках без весов молчит.
      Замер оценки на 6369 словах, где известна и она, и разметка: порог 0.70
      даёт 83% бытовых, 0.80 — 88% при базовой доле 58%; RMSE 0.35.
      Отдельный результат — ДВЕ ОТВЕРГНУТЫЕ ГИПОТЕЗЫ: ни доля выходов слова в
      четвёрках категории, ни «слово — ядро другой категории» наш статус связи
      не предсказывают (на 1948 общих связях улика у approved 0.32, у
      hard_only 0.43 — в обратную сторону). Поэтому слой «верно, но не первая
      мысль» у чужой половины объявлен отсутствующим, а не выдуман.
      Прогон всей кривой (`scripts/compare_source_decades.ts`, 1-100 на трёх
      источниках): 100/100 уровней на каждом источнике, длина слова из предела
      декады не вышла ни разу, полная приёмка декады — 6/10 у нашей базы,
      9/10 у словаря оригинала, 7/10 у сводной (падают META_RANGE и
      CATEGORY_MEAN — те же, что падали и до неё).
      PLAY-проверено в браузере: переключатель грузит сводную базу отдельным
      чанком, блок 1-10 собирается 10 из 10 за 444 мс, все уровни PASS.
      Слабое место видно там же и записано честно: у чужих категорий никто не
      размечал очевидность связи, поэтому в COMPASS может приехать `housing` —
      слово расхожее (вес прошёл), но как «часть компаса» неочевидное.

- [x] Рецепты структуры базы оригинала в генераторе (03.08, инструмент 1.11.0,
      генератор gen-1.3). Замер всех 1025 уровней bwj-org переведён в правила
      сборки — пять пунктов:
      1) четвёрка собирается градиентом очевидности «2+1+1» (замер 9987
         четвёрок: 0.71/0.60/0.50/0.39) — лечит и плоские четвёрки, и
         «в COMPASS приехал housing»;
      2) повторы слов планируются В НОВУЮ категорию по коридору декады
         (у оригинала это главный растущий рычаг: 12% виденных слов на L1-20 →
         ~80% после L400, из них 70-80% в новой категории);
      3) категория, вернувшаяся после окна свежести, пересдаётся новой
         четвёркой — не больше одного слова из прошлой (в оригинале 1725
         категорий живут с 2+ наборами, «violin» — 21 вариант);
      4) мета-цепь резервируется только из ставимых пар — найден и починен
         хронический провал META_RANGE: planDeepChain резервировал пару
         sauces → condiments, запрещённую базой (do_not_pair), и первый
         уровень блока каждый раз оставался с 0-1 мета-парами; заодно план
         ритма обязан попадать в среднее декады (лечит CATEGORY_MEAN на 51-60,
         падавший на всех трёх источниках одинаково);
      5) калибровка остаётся на L1-199: с ~L300 статистика выгрузки замерзает
         (среднее 12.4 категории десятками декад) — это почерк их генератора,
         копировать его незачем; зафиксировано в decadeProfiles.ts.
      Проверено прогоном кривой 1-100 на всех трёх источниках
      (compare_source_decades.ts): раздельные декады — наша база 6/10 → 10/10
      декад с полной приёмкой, словарь оригинала 9/10 → 10/10, сводная
      7/10 → 9/10 (остался ZIPF_BLOCK_MEDIAN на 31-40: 4.07 при цели
      3.85±0.20, промах 0.02). Новый режим «сквозная история» (декады подряд,
      как играет игрок) показал повторы как у оригинала (0.9 → 23 на уровень
      по кривой на сводной базе, 100/100 уровней, 9/10 декад) и честный предел
      нашей базы: на длинной кривой со свежестью она собирает 71/100 —
      дефицит контента, а не правил. Тесты 153/153.

## Впереди (план из блока 00 PLANNING)

- [ ] Играбельный прототип механики (4-6 h)
- [ ] MD-файлы для солвера уровней (2 h)
- [ ] Скилл Level Solver (4 h)
- [ ] MD-файлы для генератора уровней (2 h)
- [ ] Скилл Level Generator (4 h)
- [ ] Скилл оценки сложности уровней (3 h)
- [ ] Скилл JSON Export (2 h)
- [ ] Финал: 10 уровней с оценками, описание AI-воркфлоу, сайт-отчёт целиком (8 h)

## Решения

- Подход к генератору уровней зафиксирован в `approach.md` (29.07): гибрид
  (LLM придумывает, база+фильтры проверяют), слова-ловушки с ростом сложности,
  параметры поля снимаем с записей референса (ждём материалы в `reference/`).

- Хостинг инструмента: статическая страница на surge + серверный прокси для ключа AI.
- Домен инструмента: отдельный, не смешиваем с другими проектами.
- Репозиторий: https://github.com/sergemeleshchuk-netizen/bubble-word (приватный; перед сдачей открыть доступ проверяющему или сделать публичным).

# Прогон run-002-legacy-merge

Прежняя база на 306 категорий втянута в SQLite как обычный источник.
Разметка — `spec.jsonl` (руками), расширение на пулы — `scripts/build_legacy_run.py`.

- категорий создано: **156**
- категорий пополнено словами: **150**
- связей записано: **989**

## Статусы связей

Статус выведен политикой из силы связи и очевидности — тем же порогом,
что применяется к прогонам AI. Семантическая ось (`semantic_status`)
остаётся `unreviewed`: глазами эти связи никто не смотрел.

- approved: 907
- alternative: 82

## Что не пошло в базу

- связей пропущено: единственное/множественное: 10
- связей пропущено: слово уже в категории: 883

Подробно:

- bowling_words: 'pins' не добавлено — в пуле уже pin (то же слово в другом числе)
- clothing_items: 'socks' не добавлено — в пуле уже sock (то же слово в другом числе)
- face_parts: 'eyes' не добавлено — в пуле уже eye (то же слово в другом числе)
- face_parts: 'cheeks' не добавлено — в пуле уже cheek (то же слово в другом числе)
- face_parts: 'eyebrows' не добавлено — в пуле уже eyebrow (то же слово в другом числе)
- face_parts: 'lips' не добавлено — в пуле уже lip (то же слово в другом числе)
- spices_and_herbs: 'chives' не добавлено — в пуле уже chive (то же слово в другом числе)
- halloween_things: 'ghosts' не добавлено — в пуле уже ghost (то же слово в другом числе)
- jewelry: 'earrings' не добавлено — в пуле уже earring (то же слово в другом числе)
- toys: 'marbles' не добавлено — в пуле уже marble (то же слово в другом числе)

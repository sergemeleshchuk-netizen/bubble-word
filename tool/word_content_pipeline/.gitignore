.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# database/content.sqlite НЕ игнорируется: это наш авторский набор, он лежит в git
# и развивается по шагам. Раньше он был здесь, а версионировалась его копия в
# БАЗА-СЛОВ/ — то есть настоящая база не имела истории, а история была у копии.
# Пересобирается из data/*.jsonl скриптом scripts/rebuild_all.sh.

# Служебные файлы и промежуточные выгрузки
database/*.sqlite-journal
database/*.sqlite-shm
database/*.sqlite-wal
data/review_candidates.csv
data/generated_*.jsonl
data/gen_*.jsonl
data/checkpoint*.txt
data/ai_review.jsonl
database/backup/
